# Dependencies
node_modules/
.pnp
.pnp.js

# Environment — never commit real secrets (SRS §27)
.env
.env.local
.env.*.local
!.env.example

# Runtime storage (SRS §26)
storage/logs/
storage/uploads/
storage/backups/
storage/tmp/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
backend/dist/
frontend/.next/
frontend/out/
frontend/build/
.turbo/

# Test artefacts
coverage/
.nyc_output/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Temporary
*.tmp
*.bak
tmp/

# The jest harness's raw run output — regenerated by globalSetup on every `npm test`.
# `tests/baseline.json` is NOT ignored: it is the recorded per-suite assertion count the run is
# checked against, and it only changes when `npm run test:baseline` is run deliberately.
backend/tests/.last-run.json
frontend/tsconfig.tsbuildinfo
