# §36 findings — triage verdicts

Eight read-only agents verified the 61 findings left untriaged after the high-severity pass, each
reading the SRS clause and the code it names. **61 verdicts: 44 real-fixable, 11 real-blocked,
1 already-fixed, 5 refused.**

**A verdict is still a claim.** Every one acted on so far was re-verified by hand first, and that
matters: the triage itself surfaced a finding proving that an earlier *correction* of mine was based
on a false premise (finding 5 — the settings permission that does exist).

`real-blocked` means the defect is real but fixing it requires a decision the SRS does not supply.
That verdict is not a dodge; it is the honest end state for a source that names a capability without
specifying it, and eleven findings land there.

## Reclassifications made when acting on these verdicts

A verdict is a claim. Two `real-fixable` verdicts were **wrong about being fixable** — the defect was
real in both cases, but building it meant deciding something the SRS does not decide. Both are
therefore `real-blocked`:

| # | Was | Is | Why |
|---|---|---|---|
| 16 | real-fixable | **real-blocked** | Student "Documents" genuinely has no code path — the `STUDENT_DOCUMENT` upload profile cites FR-STUDENT-001 in its own rule and has never had a caller. But the `documents` table's `document_type` enum holds only *generated* kinds, its columns (`is_generated`, `generation_payload`, `generated_at`) are shaped for generation, and the only view key in the fixed 109 is `documents.view` — "View **generated** documents". §15.1 says "Documents" and nothing more. An uploaded certificate would need a type and a permission the source never supplies. |
| 17 | real-fixable | **real-blocked** | `class_id` is optional on admission where SRS:818 says "Student is assigned to a Class and Section". But §15.1 lists **Admission**, **Class Assignment** and **Section Assignment** as three separate features — which is what the code implements — and `verify-students.js:122` deliberately asserts the permissive contract. The source answers the question two ways. |

The pattern is now consistent enough to name: **where the SRS lists a capability without specifying
it, a "fix" is a specification decision wearing implementation clothes.** Thirteen findings sit there.

## All 44 `real-fixable` findings have now been applied

The last seven landed in session 26 part 27. **Three of them were not documentation**, though a
summary written at the end of part 26 called all seven *"documentation-accuracy of the same kind"* —
29, 30 and 31 together were a screen running one of §22's seven reports and offering none of its three
export formats. Reading the verdicts rather than that summary is what corrected it, and it is the
reason a verdict should be re-read before it is acted on rather than after.

Two corrections were made to the verdicts themselves while applying them, both against the source:

| # | The verdict said | The source says |
|---|---|---|
| 59 | FR-EXAM-004's Actor line is SRS:1026 | :1026 is *Preconditions*. The Actor line is **:1025** |
| 30 | the six 200s prove the `isPlatform` short-circuit | They do not — deleting it leaves all six answering 200, because the school named is subscribed to Reports on its own merits. What the short-circuit governs is the refusal a caller with **no** school sees, which is what the three assertions below it measure. Established by breaking the source, not by reading it |

What remains open is the **13 `real-blocked`** findings above and below, each needing a decision the
SRS does not supply.

## The 13 `real-blocked` findings were re-triaged adversarially, and none was overturned

Twenty-eight agents: one per blocked finding, tasked with **refuting** its verdict by finding a fix
that invents nothing, and two skeptics behind every claimed fix. Six refuters claimed a fix existed —
findings **11, 16, 17, 18, 47 and 60** — and all six were refuted, nine votes to none.

The refutations were substantive, not pedantic. Student "Documents" (16) clears the §29/§35 lens
outright — `document_type` is nullable and `models/other.js:455-457` says *"null for a plain
upload"*, so the earlier verdict's "an uploaded certificate would need a type" is itself wrong — and
the finding stays blocked anyway, on two things nobody has decided: `documents.title` is NOT NULL with
no default, so every upload must originate a human-authored value the SRS never specifies; and
writing `owner_type='student'` rows makes a child's uploaded paperwork readable by that child and
every linked parent through `selfScope()`, which is a §35 "additional security policy".

**The blocked verdict is now the more evidenced of the two, and the pattern named at the top of this
file survives a second, adversarial pass:** where the SRS lists a capability without specifying it, a
fix is a specification decision wearing implementation clothes.

**Twelve false statements were corrected without touching any of the blocked decisions**, including a
422 message that told a Super Admin to configure a negotiated price with a `subscription_overrides`
row nothing reads, and a fabricated SRS quotation in `payments.routes.js:35`. Those are recorded in
IMPLEMENTATION_PROGRESS.md, session 26 part 28.

**Two claims in this file are themselves out of date and are corrected here rather than left:**

| Where | Said | Actually |
|---|---|---|
| finding 16 (Reclassifications table, above) | "An uploaded certificate would need a type … the source never supplies" | `documents.document_type` is `allowNull: true, defaultValue: null` and the model comment reads "null for a plain upload" — the type is already decided. The finding stays blocked on `title` and on read scope, not on the type |
| finding 17 | "there is no admission form: `frontend/src/app/(school)/school/students/` contains `page.tsx` only" | The admission form was built in session 26 part 26. The class-less admission is therefore reachable from the shipped UI, not only from the API — the finding's exposure is **understated**, not overstated |

A caution for anyone acting on this file: an agent re-triaging finding 47 quoted a sentence from
`frontend/src/app/(teacher)/teacher/page.tsx` that **does not exist in the project**, and built a
correction on it. Every correction applied from this pass was re-read at first hand first, and the
ones that could not be confirmed were dropped. A verdict is a claim; so is a quotation.

## real-fixable (44)

### 5 — §5 role table (SRS-extracted.md:96) / FR-AUTH-009 catalogue (SRS-extracted.md:190)

The claim under triage is not "Settings is unspecified" (already recorded) but that the *stated justification* rests on a false fact. Verified: the catalogue at backend/src/config/permissions.js contains exactly 109 entries (grep -c "^  { key: '" → 109), and entry 14 is `{ key: 'settings.platform.manage', group: 'Platform', name: 'Manage global settings', module: null }` at line 39. It is granted to super_admin through `[ROLES.SUPER_ADMIN]: ALL` at permissions.js:254. A grep across backend/src, backend/scripts, frontend/src and docs returns exactly one non-docs hit — its own definition line — so it is an orphan key inside the SRS-fixed 109. Three places state the opposite: docs/IMPLEMENTATION_CHECKLIST.md:625 ("no settings permission exists in the fixed catalogue of 109 — which is why the nav entry borrows `schools.view`"), frontend/src/lib/nav.ts:104, and frontend/src/app/(platform)/super-admin/settings/page.tsx:17 — with page.tsx:64 rendering the false statement to the signed-in user. The permission name ("Manage global settings") is a near-verbatim match for SRS:96's "global settings". The fix invents nothing: it corrects a factual error and, optionally, points the nav entry at the key that already exists and is already granted — no 110th permission, no new grant, no new table.

**Consequence:** A false statement is rendered to the viewer in the shipped UI (settings/page.tsx:64). Behaviourally, because the entry gates on the borrowed `schools.view` — which organization_admin holds (permissions.js:262) — an organization_admin sees a Settings entry into the platform surface, whereas `settings.platform.manage` reaches super_admin alone.

**Fix / open question:** Delete the false clause from all three places (IMPLEMENTATION_CHECKLIST.md:625, nav.ts:104, settings/page.tsx:17 and the user-visible copy at :64); it is simply untrue. Then set nav.ts:109 to `permission: 'settings.platform.manage'` — the exact key already in the fixed 109 and already granted to super_admin via ALL. The remaining §33-names-Settings-but-§9-specifies-nothing gap is untouched by this and stays recorded.

**Evidence:** SRS: docs/SRS-extracted.md:96 "| Super Admin | Platform-level owner … and global settings (see Section 9). |". CODE (the permission claimed not to exist): backend/src/config/permissions.js:39; granted at backend/src/config/permissions.js:254 "[ROLES.SUPER_ADMIN]: ALL,". FALSE CLAIMS: docs/IMPLEMENTATION_CHECKLIST.md:625; frontend/src/lib/nav.ts:104; frontend/src/app/(platform)/super-admin/settings/page.tsx:17 and :64. CONSUMER: frontend/src/lib/nav.ts:109 "{ label: 'Settings', href: '/super-admin/settings', permission: 'schools.view' },"; `schools.view` is granted to organization_admin at backend/src/config/permissions.js:262.

---

### 6 — §7 FR-AUTH-009 — Permission Middleware (SRS-extracted.md:189-195)

Counted directly: `grep -o "group: '[^']*'" backend/src/config/permissions.js | wc -l` → 109, `| sort -u | wc -l` → 22 (AI, Academic, Assignments, Attendance, Billing, Documents, Exams, Fees, Finance, Homework, Library, Notifications, Operations, Organizations, People, Platform, Reports, School, Schools, Subscriptions, Timetable, Users). `permissionCatalogue()` keys a Map strictly on `permission.group` and emits one entry per key, with no filtering, so `GET /users/permissions` returns 22 groups, not 24. The key count (109) in the same checklist cell is right; only the group count is wrong. The repository's own other record agrees with 22: IMPLEMENTATION_PROGRESS.md:280 says "109 rows across 22 groups". The finding's secondary point also holds — verify-users-roles.js asserts `dataOf(catalogue).total === PERMISSION_KEYS.length` and that every entry carries a key and a name, but never asserts a group count, so the wrong figure was never pinned by a check.

**Consequence:** No runtime effect — the endpoint is correct; the document is wrong. A reader of the checklist expects two groups that do not exist, and the two project documents disagree with each other about the same endpoint.

**Fix / open question:** Change "24 groups" to "22 groups" at docs/IMPLEMENTATION_CHECKLIST.md:295, matching IMPLEMENTATION_PROGRESS.md:280. If the figure is to stay honest, add one assertion in backend/scripts/verify-users-roles.js beside the existing total check — `dataOf(catalogue).groups.length` against the distinct-group count derived from PERMISSIONS — which pins it without adding a permission, table or column.

**Evidence:** SRS: docs/SRS-extracted.md:190 (FR-AUTH-009, requests checked against granted permissions). CODE: backend/src/config/permissions.js — 109 `group:` labels, 22 distinct; grouping is by that field alone at backend/src/modules/users/users.service.js:474 "if (!byGroup.has(permission.group)) byGroup.set(permission.group, []);" and :482 "return [...byGroup.entries()].map(([group, permissions]) => ({ group, permissions }));". CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:295 "FR-AUTH-009's catalogue — 109 keys in 24 groups". CONTRADICTED BY: IMPLEMENTATION_PROGRESS.md:280 "109 rows across 22 groups". SUITE: backend/scripts/verify-users-roles.js:838-851 (total and per-entry shape only; no group-count assertion).

---

### 7 — §7 / §8 — rows 3.B (docs/IMPLEMENTATION_CHECKLIST.md:205-214) and 3.C (:224-227), governed by the legend at :29-31

Both preamble statements are false against the repository as it stands. `ls backend/tests/` returns baseline.json, globalSetup.js, helpers/, setup.js, verify.test.js — so "`tests/` does not exist" and "`tests/setup.js` … is absent" are both wrong. backend/package.json carries a complete `jest` block (lines 66-79) pointing at `<rootDir>/tests/globalSetup.js` and `<rootDir>/tests/setup.js`, so the config is not dangling either. The same file contradicts itself twice more: :79 "That runs **all 38** suites against `msms_test`" and row 6.17 at :840 "**Done. `npm test` runs 5,338 jest tests, 0 failures, exit 0**". The count errors are real too: :819 says "the thirteen standalone `scripts/verify-*.js` suites" and :36 says "one of the thirty", while `ls backend/scripts/verify-*.js | wc -l` → 38. Two small corrections to the finding's own citations, which do not change the outcome: the legend lines are :29-31 not :30-31, the "thirty" is at :36 not :37, and row 6.17 now reads 5,338 rather than the 5,329 the finding quotes (the checklist moved after the finding was written). This matters exactly as the finding says: the legend at :29 defines `Completed` as "an executable check covers it and that check passes today", and the file gives three mutually exclusive answers about which harness that is.

**Consequence:** No runtime effect. A reader of any §7/§8 status cannot tell what evidence backs it: the two governing preambles say the harness does not exist and `npm test` fails, while the same file elsewhere reports 38 suites and 5,338 passing jest tests. The suite-count figures (thirteen, thirty) understate the 38 on disk by up to a factor of three.

**Fix / open question:** Delete or rewrite the two stale preamble paragraphs (docs/IMPLEMENTATION_CHECKLIST.md:104-106 and :817-820) to state what is true — `backend/tests/` exists, the `jest` block in package.json resolves, and `npm test` runs the fold recorded in row 6.17. Replace "thirteen" at :819 and "thirty" at :36 with 38, matching :79. Purely factual corrections against the repository; no SRS decision involved. Note that :37's "4,613 assertions" is stale by the same mechanism (row 6.17 and the findings header both say 5,133 assertions across the 38 suites) and should be corrected in the same pass.

**Evidence:** CHECKLIST (false): docs/IMPLEMENTATION_CHECKLIST.md:104-106 "**There is no jest test suite.** `tests/` does not exist and `npm test` fails."; :817-820 "**There is no jest suite.** `tests/` does not exist, `tests/setup.js` is referenced by the jest config in `package.json` but absent … Every status below refers to the thirteen standalone `scripts/verify-*.js` suites". CHECKLIST (self-contradicting): :36 "one of the thirty `backend/scripts/verify-*.js` suites"; :79 "That runs **all 38** suites against `msms_test`"; :840 row 6.17 "**Done. `npm test` runs 5,338 jest tests, 0 failures, exit 0**". REPO: backend/tests/ → baseline.json, globalSetup.js, helpers/, setup.js, verify.test.js; backend/package.json:66-79 the `"jest": { "globalSetup": "<rootDir>/tests/globalSetup.js", "setupFilesAfterEnv": ["<rootDir>/tests/setup.js"], … }` block; `ls backend/scripts/verify-*.js | wc -l` → 38. Legend: docs/IMPLEMENTATION_CHECKLIST.md:29 "`Completed` | The code exists **and** an executable check covers it **and** that check passes today".

---

### 8 — §9.1 (SRS-extracted.md:257-268), FR-SADMIN-001 (SRS-extracted.md:292-298), §33 Super Admin screen list (SRS-extracted.md:1578-1595)

Confirmed on both halves. The backend is complete: platform.service.js returns all eleven metrics in SRS §9.1's source order at lines 167-179 (totalOrganizations … pendingPayments), served by `router.get('/dashboard', requirePermission('platform.dashboard.view'), …)` at platform.routes.js:34 — so checklist:246's FR-SADMIN-001 "Completed", scoped to the endpoint, is accurate. The screen is not: frontend/src/app/(platform)/super-admin/page.tsx renders a heading, `profile?.user.name`, and a sentence saying the eleven metrics "are not rendered yet". A grep of the whole of frontend/src for `platform/dashboard`, `totalOrganizations`, `totalSchools`, `monthlyRevenue`, `pendingPayments` and for the SRS's own labels ("Total Organizations", "Active Subscriptions", "Monthly Revenue") matches only that page's comment and copy — nothing fetches or renders a single metric. §33:1579 lists Dashboard as the first of the sixteen Super Admin screens, so it is in scope for row 4.3. Row 4.3's cell claims "All sixteen exist and are reachable — **fifteen functional**", which cannot be true when the dashboard is a placeholder and Settings is a gap statement: fourteen at most. The row's own prose concedes it two lines later ("the dashboard placeholder", :627). The second inconsistency is real too: :652 says "The row stays In Progress rather than Completed" while row 4.3's Status cell reads "Completed".

**Consequence:** A Super Admin signing in lands on §33's first screen and sees a name and a disclaimer — FR-SADMIN-001's stated outcome, "views a consolidated platform overview", is not delivered anywhere in the UI even though the data is one authenticated GET away. The checklist counts that screen among the working ones, so the row overstates by one and its status word contradicts its own prose.

**Fix / open question:** Two independent parts, neither requiring invention. (1) Documentation: change "fifteen functional" to "fourteen" at docs/IMPLEMENTATION_CHECKLIST.md:625, naming the dashboard placeholder as the second non-functional screen alongside Settings, and reconcile the Status cell with :652 (pick one — the prose says In Progress). (2) Code: render the eleven on frontend/src/app/(platform)/super-admin/page.tsx from `GET /platform/dashboard`. The field list, the order and the endpoint are all fixed already — SRS:297 names the eleven, platform.service.js:167-179 returns exactly those keys in that order, and `platform.dashboard.view` already exists in the 109. No new route, permission, table or invented metric.

**Evidence:** SRS: docs/SRS-extracted.md:257 "The Super Admin Dashboard displays:" with the eleven at :258-268; :297 "- System aggregates and displays: Total Organizations, Total Schools, Active Schools, Suspended Schools, Total Students, Total Teachers, Active Subscriptions, Expired Subscriptions, Monthly Revenue, Yearly Revenue, and Pending Payments."; :298 "Expected Outcome: Super Admin views a consolidated platform overview."; :1579 "- Dashboard" under §33 "## Super Admin". CODE (backend, correct): backend/src/modules/platform/platform.service.js:167-179; backend/src/modules/platform/platform.routes.js:34. CODE (frontend, the gap): frontend/src/app/(platform)/super-admin/page.tsx:20-22 "FR-SADMIN-001&rsquo;s eleven metrics are served by <code>GET /platform/dashboard</code> and are not rendered yet." — the whole component is 11 lines of JSX with no fetch. CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:625 "All sixteen exist and are reachable — fifteen functional" vs :627 "Six by hand — the dashboard placeholder and **Schools**, the exemplar" vs :652 "The row stays In Progress rather than Completed" against the same row's Status cell "Completed".

---

### 14 — FR-SUB-012 / §12.2 (SRS-extracted.md:586-592)

Confirmed on every point, and the code's own header states the intended behaviour that the code does not implement. FR-SUB-012's Expected Outcome is that the subscription ENTERS a Grace Period state of the configured duration before further transition, and its Description says the grace period is applied 'after a subscription becomes past due or expires'. Two sweep passes make a subscription past due. Pass 3 (period lapsed) does it correctly: applyState(PAST_DUE) at :2413-2421, then a second applyState(GRACE_PERIOD) at :2429-2436 when graceDays > 0. Pass 1 (trial ended) does not: :2364-2377 writes only { state: PAST_DUE, columns: { grace_period_ends_at } } with event TRIAL_ENDED and stops. Nothing else can move that row into grace: :2431 is the SOLE writer of STATES.GRACE_PERIOD anywhere in src/ (grep over src/ returns only constants.js:296,311, the transition maps at subscriptions.service.js:964,1073,1575 which list it as a FROM state for suspend/cancel/renew, :2431 as the writer, and :2444 as a query filter), and pass 3's query at :2402-2408 selects only state IN (active, expiring), which a trial-derived past_due row is not. Pass 4 at :2440-2447 then matches { state: PAST_DUE, grace_period_ends_at <= at } and writes EXPIRED with the note 'Grace period ended (SRS §12.2, FR-SUB-012)' — for a subscription that never held the state. grace_period_days is inherited from the plan at :690/:716, so this fires for any plan on which a Super Admin configured a grace period per FR-SUB-012. The sweep's own header at :2236-2239 asserts the opposite of what the code does — 'an unpaid trial becomes Past Due, which pass 3 then takes into Grace Period' — and :2245 describes pass 3's input as "'active'/'expiring'/'trial'-derived past due". That comment is a claim, not evidence, but it is a false claim, which is why this reads as an omission rather than a design choice. The fix invents nothing: append to pass 1's handler the same conditional second applyState pass 3 already performs (state GRACE_PERIOD, event GRACE_PERIOD_STARTED, when Number(grace_period_days) > 0), or add STATES.TRIAL to pass 3's input and let one pass own the transition. Same SRS clause, same helper, same event constant.

**Consequence:** Observable, though not in entitlement: past_due and grace_period are both in SUBSCRIPTION_USABLE_STATES (constants.js:307-313), so the school keeps access either way. What differs is what anyone looking at the subscription sees. The Super Admin subscriptions screen offers a grace_period state filter (frontend/src/app/(platform)/super-admin/subscriptions/page.tsx:95-105) that a trial-lapsed school in its grace window will never match; its StatusBadge reads 'Past Due' for the whole grace period; and its subscription_history carries no grace_period_started row, so the audit trail shows trial_ended → expired with an EXPIRED note that says the grace period ended. FR-SUB-010's outcome ('state accurately reflects its current standing at all times') is not met for the trial path.

**Fix / open question:** In pass 1 at backend/src/modules/subscriptions/subscriptions.service.js:2364-2377, after the existing applyState to PAST_DUE, add the same block pass 3 runs at :2428-2437: if Number(subscription.grace_period_days) > 0, applyState({ state: STATES.GRACE_PERIOD, columns: {}, event: EVENTS.GRACE_PERIOD_STARTED, notes: `${graceDays}-day grace period (SRS §12.2)` }) and increment report.graceStarted. Equivalent alternative: add STATES.TRIAL-derived rows to pass 3 rather than duplicating the block. Either way the sweep header at :2236-2239 becomes true instead of false. No new state, column, event or rule is introduced.

**Evidence:** SRS: docs/SRS-extracted.md:588 'Description: Super Admin configures the grace period applied after a subscription becomes past due or expires.'; :592 'Expected Outcome: Subscription enters a Grace Period of the configured duration before further state transition (e.g., to Expired/Suspended).' CODE: backend/src/modules/subscriptions/subscriptions.service.js:2364-2377 (pass 1, PAST_DUE only, no second applyState); :2411-2437 (pass 3, PAST_DUE then GRACE_PERIOD at :2431, report.graceStarted at :2436); :2402-2408 (pass 3 query, state IN [ACTIVE, EXPIRING]); :2440-2447 (pass 4, matches PAST_DUE with grace_period_ends_at <= at); :2236-2239 and :2245 (header comment claiming pass 3 takes trial-derived rows into grace); :690,:716 (grace_period_days inherited from the plan); backend/src/config/constants.js:296,306-313 (GRACE_PERIOD state, SUBSCRIPTION_USABLE_STATES).

---

### 16 — §15.1 "Documents" (SRS-extracted.md:780); FR-STUDENT-001 Functional Behavior (SRS-extracted.md:817)

The SRS requires the admission flow to capture Documents as well as the Photo (SRS-extracted.md:817 "- System captures Student Photo and Documents.", with "- Documents" listed as its own §15.1 feature at :780). The claim that no code path exists is confirmed: a whole-repo grep for STUDENT_DOCUMENT / student_document returns exactly two hits inside backend/src — the profile key at constants.js:803 and the rule at constants.js:848-852 — plus the middleware harness backend/scripts/verify-middlewares.js:460,461,1266,1271 and prose in docs. Cross-checked the other way: every UPLOAD_PROFILES.* reference in backend/src/modules is payments.routes.js:122 (PAYMENT_PROOF), assignments.routes.js:156 (SUBMISSION), ai.routes.js:115 (AI_SOURCE), students.routes.js:92 (PERSON_PHOTO), homework.routes.js:79 (HOMEWORK). student_document is the one profile of six with no product caller. The §20.5 module is genuinely not a substitute: documents.routes.js declares only three routes (GET / at :66, POST / 'documents.generate' at :74, GET /:id at :82) and imports no upload middleware, and §20.5 itself (SRS-extracted.md:1071-1079) and FR-DOC-001 (:1130-1136) describe generation of seven artefacts only — no upload. Fixable without inventing anything: the fixed §29 schema already has the home for it — models/other.js:466 makes document_type nullable, :468-469 give owner_type/owner_id with DOCUMENT_OWNER_TYPES.STUDENT at constants.js:585, :471-475 are file_path/file_name/mime_type/file_size_bytes and :476-481 is is_generated (false = uploaded). No 65th table, column, index or permission is needed, and no seed value is invented. One qualification on the second half of the claim: the checklist row does NOT hide the gap — IMPLEMENTATION_CHECKLIST.md:521 ends '**Not covered:** §15.1's "Documents" — the `student_document` profile still has no caller', so the 'overstates' framing is the weak part of the finding; the code gap is the real part.

**Consequence:** A school cannot attach a birth certificate, transfer certificate or any other admission paperwork to a student through the product at all — the FR's Documents clause has no API and no screen. Half of one Functional Behavior line of FR-STUDENT-001 is unbuilt.

**Fix / open question:** Mount the one missing caller, mirroring the PERSON_PHOTO route shipped for the same FR: `POST /students/:id/documents` in backend/src/modules/students/students.routes.js guarded by `requirePermission('students.manage')` (the precedent and rationale are already recorded at students.routes.js:74-88) with `uploadArray(UPLOAD_PROFILES.STUDENT_DOCUMENT, 'documents')` from upload.js:503, writing one `documents` row per file with owner_type='student', owner_id=:id, document_type=null, is_generated=false, file_path/file_name/mime_type/file_size_bytes from req.files, uploaded_by=req.user.id and title from the original filename. Do not increment storage_limit: no existing upload caller does (usageService.recordUsage has exactly one call site, ai.service.js:431), so that is a separate, module-wide question. Do not add a DOCUMENT_TYPES value — §20.5's seven are generated artefacts and document_type is already nullable for uploads.

**Evidence:** SRS: docs/SRS-extracted.md:780 "- Documents"; docs/SRS-extracted.md:817 "- System captures Student Photo and Documents." | CODE: backend/src/config/constants.js:803 `STUDENT_DOCUMENT: 'student_document',` and :848-852 the frozen rule citing '§15.1 / FR-STUDENT-001 — "Documents"' — the only two hits in backend/src; backend/src/modules/documents/documents.routes.js:66,74,82 (three routes, none an upload); backend/src/middlewares/upload.js:503 `function uploadArray(profile, field, maxCount, options = {})` exists and is unused by any module; backend/src/models/other.js:465-481 (nullable document_type, owner_type/owner_id, file_* columns, is_generated) | CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:521

---

### 17 — FR-STUDENT-001 (SRS-extracted.md:812, :816, :818); §15.1 (SRS-extracted.md:781-784)

Confirmed on both sides. SRS-extracted.md:816 states unconditionally '- System assigns a Student ID and Roll Number.' and :818 '- Student is assigned to a Class and Section.'; :812 makes 'Class and section exist' a precondition. The create schema at backend/src/modules/students/students.validation.js:124-134 marks only `first_name` (:127) and `admission_date` (:128) `.required()`; `class_id` (:129) and `section_id` (:130) are optional, and resolvePlacement (students.service.js:186-226) only validates them `if (classId)`. On that path students.service.js:425 calls `allocateRollNumber(school.id, placement.classId, ...)`, which returns null at students.service.js:340 (`if (!classId) return null;`) and the row is written with roll_number null (models/people.js:49 `roll_number: ... allowNull: true`). student_id is still allocated at :423-424, so the two halves of :816 do behave differently, as claimed. The claim is also stronger than the finding states: nothing back-fills it later. `update()` writes the checked class_id at students.service.js:474 without ever calling allocateRollNumber — only `applyTransition('promote')` re-allocates (:562), and promotion additionally refuses a class equal to the current one (:530-534). The validation docstring at students.validation.js:117-123 ('the service allocates one ... The same applies to `roll_number`') and IMPLEMENTATION_CHECKLIST.md:521 ('Student ID and Roll Number are assigned when omitted') both state this unconditionally, which is false for the unplaced path. Only reachable via the API today (there is no admission form: frontend/src/app/(school)/school/students/ contains page.tsx only, and its 'Admit student' link at page.tsx:301 points at a non-existent /school/students/new), but the API is a delivered surface and verify-students.js never exercises an admission without a class.

**Consequence:** `POST /students` with only first_name + admission_date returns 201 for a student who has no class, no section and no roll number, and no later PATCH gives them one. They render with '—' in the Roll column (frontend/src/app/(school)/school/students/page.tsx:231), never match the roster's roll-number search (students.service.js:399), and appear in no class-filtered list — a permanently unplaced, unnumbered child in the roster.

**Fix / open question:** Make `class_id` required on admission: `class_id: fields.class_id.required(),` at backend/src/modules/students/students.validation.js:129. That is exactly what :818 states and it makes :816's roll-number half unconditional, because allocateRollNumber then always has a scope. Leave `section_id` optional — that is the status quo, allocateRollNumber already scopes a section-less class explicitly (students.service.js:343-345 `section_id: { [Op.is]: null }`) and promotion deliberately drops the section on a class change (students.service.js:191-211), so mandating it would need a rule the SRS does not state. Then correct the docstring at students.validation.js:117-123, which currently claims roll_number is always allocated when omitted, and the same phrase in IMPLEMENTATION_CHECKLIST.md:521.

**Evidence:** SRS: docs/SRS-extracted.md:812 "Preconditions: Class and section exist.", :816 "- System assigns a Student ID and Roll Number.", :818 "- Student is assigned to a Class and Section." | CODE: backend/src/modules/students/students.validation.js:124-134 (only :127 first_name and :128 admission_date are `.required()`); backend/src/modules/students/students.service.js:339-340 `async function allocateRollNumber(schoolId, classId, sectionId, transaction) { if (!classId) return null;`, called at :425; backend/src/models/people.js:49 `roll_number: { type: DataTypes.STRING(40), allowNull: true }`; no back-fill: students.service.js:458-497 `update()` sets class_id at :474 with no roll-number allocation | CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:521

---

### 19 — FR-FEE-001 (SRS-extracted.md:918-923), '- User may configure Fine and Discount.' (:922)

Confirmed. Checklist row 3.L asserts of the pair 'Fine and Discount ... both `fixed` and `percentage` are honoured'; only Discount is. discountFor() at backend/src/modules/fees/fees.service.js:111-121 reads structure.discount_type and branches percentage → money.percentageOf(amount, discount_amount) versus fixed → money.round(discount_amount), and assign() calls it at :320-322 whenever the caller supplies no discount override. There is no counterpart for the fine: :323 is the whole of it — `const fine = money.round(payload.fine_amount ?? 0);` — taking the value verbatim from the request body and defaulting to zero, with no reference to the structure. A grep for fine_type/fine_amount/fine_grace_days over backend/src confirms the absence of any arithmetic in this module: models/finance.js:66-68 (columns) and :87-88 (a validator requiring fine_amount > 0 when fine_type is set), fees.validation.js:51-53,92-94,113-115 (schemas) and :142 (fine_amount on the assign body), fees.service.js:83-85 (the editable-field list) and :323/:339 (the verbatim copy). Every other hit is the library module's unrelated per-day fine. So a structure carrying fine_type 'percentage', fine_amount 10, assigned without an explicit fine_amount, yields fine_amount 0 on the fee; pass fine_amount: 10 and netOf() at :123-126 adds a flat 10, since net = amount − discount + fine with no type resolution anywhere. The code is candid about this in its header (:33-38) and the suite asserts the code's behaviour, not the note's: verify-fees.js:860 checks the structure's percentage discount is carried onto the fee, while :886 checks only that 'a fine set on the assignment lands on the fee'. It is the checklist sentence that drifted. Fixable because the fix is to the sentence: restate what the module does. Note the other direction is NOT available — implementing a structure-driven fine would require deciding when a fine applies, which §17 does not state (it names 'Fine' in the component list at :911 and says only 'may configure' at :922, with no trigger, no accrual, no relation to due_date), so a code fix would be blocked. Only the note is actionable.

**Consequence:** No runtime misbehaviour — the API does exactly what its own header says. The consequence is in the record: IMPLEMENTATION_CHECKLIST.md:541 tells a reader that a percentage fine configured on a structure will be applied, and it will not. Anyone configuring fine_type 'percentage'/fine_amount 10 on a structure and assigning it gets fees with fine_amount 0, or a flat 10 if they pass it, with no error to signal the divergence.

**Fix / open question:** Amend the sentence at docs/IMPLEMENTATION_CHECKLIST.md:541 to state the module's actual split, which its own header at fees.service.js:33-38 already words: the Discount is resolved from the structure and honours both `fixed` and `percentage` (discountFor(), :111-121); the Fine is configured on the structure but applied only when set explicitly on the assignment (:323), because §17 names no rule for when a fine applies and inventing an accrual trigger is what §35 forbids. Do not 'fix' the code to match the note.

**Evidence:** SRS: docs/SRS-extracted.md:922 '- User may configure Fine and Discount.'; :923 'Expected Outcome: Fee Structure is available for assignment to students.' CLAIM: docs/IMPLEMENTATION_CHECKLIST.md:541 'Fine and Discount are the "may configure" half and both `fixed` and `percentage` are honoured.' CODE, discount honoured: backend/src/modules/fees/fees.service.js:111-121 discountFor() branching on structure.discount_type, called at :320-322. CODE, fine not honoured: backend/src/modules/fees/fees.service.js:323 'const fine = money.round(payload.fine_amount ?? 0);' and :339 'fine_amount: fine,' — no read of structure.fine_amount or structure.fine_type anywhere. Grep for fine_type over backend/src: models/finance.js:67,87; fees.validation.js:52,93,114; fees.service.js:33,84 — declaration, validation and an editable-field list, no arithmetic. Module header: backend/src/modules/fees/fees.service.js:33-38. Suite agrees with the code: backend/scripts/verify-fees.js:860 versus :886.

---

### 21 — §17 status vocabulary (SRS-extracted.md:911-912 'Pending Fee' / 'Partial Payment'); STUDENT_FEE_STATUS as the module's state machine

Confirmed, with two line numbers in the finding slightly stale and the substance intact. The Fees screen's Ledger status filter offers five values (frontend/src/app/(school)/school/fees/page.tsx:217-222) — unpaid, partially_paid, paid, overdue, waived — and sends the selection as `status` (:176, `status: status || undefined`; the finding cites :206 and :173, the actual lines are :221 and :176). The ledger endpoint pins status to the enum: fees.validation.js:186 `status: Joi.string().valid(...Object.values(STUDENT_FEE_STATUS))` over constants.js:472-477, which is exactly { UNPAID, PARTIALLY_PAID, PAID, WAIVED } — no OVERDUE. The key is known and the value is off-enum, so Joi errors and validate.js:102 turns it into ApiError.validation('Validation failed'), a 422 — it is not stripped, so the request does not quietly return everything. And the 422 lands in the error branch, not the refusal branch: useCollection.ts:120-126 records in its own words that VALIDATION_ERROR 'is not one of the explained codes, so it would surface as a red error banner with a "Try again" button that is guaranteed to fail identically'. That is precisely what happens here — the offending value is still in the select, so retrying reproduces it. `overdue` is not this module's word: it belongs to INVOICE_STATUS (constants.js:372) and LIBRARY_TRANSACTION_STATUS (constants.js:549). Nothing in §17 or in student_fees derives an overdue status; the only date on the row is due_date, which no status computation reads. Fixable with no invention at all: the reachable vocabulary is Object.values(STUDENT_FEE_STATUS), and the project has already set the precedent for exactly this repair on the Students screen (IMPLEMENTATION_CHECKLIST.md:698-703 — the filter was narrowed to the three reachable STUDENT_STATUS values so an administrator could not select a value that returns nothing).

**Consequence:** One of the five options on the Fees ledger filter breaks the screen. Choosing 'Overdue' returns 422 'Validation failed', the table is replaced by the red ErrorNotice, and its 'Try again' button re-sends the identical rejected query because the selection stays in the box — the user's only way out is to change the filter. It is not a harmless empty list.

**Fix / open question:** Delete the option at frontend/src/app/(school)/school/fees/page.tsx:221, leaving the four values STUDENT_FEE_STATUS actually holds (unpaid, partially_paid, paid, waived) plus 'Any status'. This is the repair already applied to the Students screen and recorded at IMPLEMENTATION_CHECKLIST.md:698-703, and it invents nothing: the option list becomes Object.values(STUDENT_FEE_STATUS), the same list the endpoint validates against. Do not add an OVERDUE value to STUDENT_FEE_STATUS — §17 describes no overdue fee state and nothing computes one.

**Evidence:** SRS: docs/SRS-extracted.md:911-912 '- Pending Fee' / '- Partial Payment' (§17 names no overdue fee state). FRONTEND: frontend/src/app/(school)/school/fees/page.tsx:221 '<option value="overdue">Overdue</option>' within the five options at :217-222, sent at :176 'status: status || undefined'. BACKEND refuses it: backend/src/modules/fees/fees.validation.js:186 'status: Joi.string().valid(...Object.values(STUDENT_FEE_STATUS)),' over backend/src/config/constants.js:472-477 STUDENT_FEE_STATUS = { UNPAID, PARTIALLY_PAID, PAID, WAIVED }. 422 rather than strip: backend/src/middlewares/validate.js:88-102, 'if (details.length) return next(ApiError.validation(\'Validation failed\', details));'. Error banner rather than refusal: frontend/src/lib/useCollection.ts:120-126. 'overdue' belongs elsewhere: backend/src/config/constants.js:372 (INVOICE_STATUS) and :549 (LIBRARY_TRANSACTION_STATUS). Precedent for the fix: docs/IMPLEMENTATION_CHECKLIST.md:698-703. Claim: docs/IMPLEMENTATION_CHECKLIST.md:667 records the Fees screen among the 17 'Completed'.

---

### 22 — §18 (SRS-extracted.md:935-938) / FR-FIN-001 (SRS-extracted.md:945-952)

Every link in the chain checks out. The Finance screen's category control is a free-text box: frontend/src/app/(school)/school/finance/page.tsx:151-158 `<input id={`${kind}-category`} type="text" placeholder="Category…" ...>`, whose value is sent verbatim at :87 `category: category || undefined`. Backend: finance.validation.js:207 and :220 pin the list filters to `expenseFields.category` / `incomeFields.category`, defined at :94 and :152 as `Joi.string().valid(...Object.values(EXPENSE_CATEGORIES))` / `INCOME_CATEGORIES` — two values each, constants.js:480-483 (salaries, other_expenses) and :485-488 (fees, other_income), taken from §18's own list at SRS-extracted.md:935-938. A bad value is a rejection, not a strip: validate.js:102 `if (details.length) return next(ApiError.validation('Validation failed', details));` → 422 with code VALIDATION_ERROR (ApiError.js:67). And that code is not in useCollection's EXPLAINED_CODES set (lib/useCollection.ts:75-96), so it lands in `setError(caught.message)` and renders as ErrorNotice at finance/page.tsx:173, ahead of the empty state at :177-181 — with a 'Try again' button that re-issues the identical invalid query. There is no debounce anywhere in useCollection (the effect refires on every serialised query change, lib/useCollection.ts:139-176), so every intermediate keystroke is a request and a red banner. The comparison claim also holds: results, library, parents, homework, students and subjects all filter closed enums with a `<select>` seeded from a local constant list (e.g. students/page.tsx:353-372); finance is the only one that does not. Ironically the same file's own header (lib/useCollection.ts:118-129) documents this exact failure mode for over-long `q` and truncates to avoid it.

**Consequence:** An accountant typing anything into the Finance Category box watches the ledger table vanish and a red 'Validation failed / Try again' banner appear on each keystroke; only the four exact strings salaries, other_expenses, fees, other_income ever return rows, and nothing on the screen names them. Retrying cannot succeed because the offending text stays in the box.

**Fix / open question:** Replace the free-text input at frontend/src/app/(school)/school/finance/page.tsx:151-158 with a `<select>` following the house pattern (frontend/src/app/(school)/school/students/page.tsx:353-372): a local const per ledger — ['salaries','other_expenses'] for expenses and ['fees','other_income'] for incomes, mirroring EXPENSE_CATEGORIES/INCOME_CATEGORIES at backend/src/config/constants.js:480-488, which are themselves §18's four names — with `<option value="">All categories</option>` first and `setPage(1)` on change, keeping the existing `row.category.replace(/_/g, ' ')` labelling used at :113. No new constant file, no shared enum export and no schema change is required.

**Evidence:** SRS: docs/SRS-extracted.md:935-938 "- Income / - Expenses / - Salaries / - Other Expenses"; :945-952 FR-FIN-001 | CODE: frontend/src/app/(school)/school/finance/page.tsx:151-158 (free-text category input), :87 (sent as `category`), :173 ErrorNotice ahead of :177-181 empty state; backend/src/modules/finance/finance.validation.js:94, :152, :207, :220; backend/src/config/constants.js:480-488; backend/src/middlewares/validate.js:102; backend/src/utils/ApiError.js:67; frontend/src/lib/useCollection.ts:75-96 (VALIDATION_ERROR absent from EXPLAINED_CODES), :139-176 (no debounce) | CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:667 "| 4.4 | School: 17 MVP screens | §33 | Completed |", :675 the Finance row of the hand-built table, neither noting this

---

### 23 — §17 / FR-FEE-001, FR-FEE-002 (SRS-extracted.md:915-932)

Confirmed end to end. The Fees screen sends `q` on two of its three tabs: frontend/src/app/(school)/school/fees/page.tsx:113 `useMemo(() => ({ page, limit: 20, q: search || undefined }), [page, search])` feeding `/fees/structures` at :114 (input placeholder 'Name or component…' at :144), and the same at :249-250 for `/fees/payments` (placeholder 'Receipt number or reference…' at :271). The backend accepts it: fees.validation.js:169, :192 build both list schemas with `listQuery(...)`, which concatenates `commonSchemas.search` (validate.js:167-177 over :157 `search: Joi.object({ q: Joi.string().trim().allow('').max(120) })`), so `q` validates and survives onto req.query. The controller passes req.query straight through (fees.controller.js:10, :90). And then it is dropped: fees.service.js:162-178 `listStructures` builds `where` from component/class_id/academic_session_id/is_active only, and :587-609 `listPayments` from student_id/student_fee_id/method/receipt_number/from/to only — a grep for `q` in the whole of fees.service.js returns nothing. Nothing generic rescues it either: paginateQuery (utils/pagination.js:72-84) only applies limit/offset. So the request returns 200 with the complete unfiltered page. The named contrast is real and quotable: finance.service.js:304-318 carries exactly the sentence the finding quotes and then implements `q` over title/subcategory/reference, noting that students, teachers, staff and parents already do the same. The ledger tab is correctly excluded from the claim — it has no search box (fees/page.tsx:176) and listLedger (fees.service.js:396) reads no q. Second-order confirmation of the intent: listPayments does support an exact `receipt_number` filter (:596), but the screen's receipt box sends its text as `q`, so even an exact receipt number typed into the box labelled 'Receipt number' does not filter.

**Consequence:** A silent wrong answer rather than an error: typing 'Transport' into the fee-structure search returns every structure with 200 OK, and typing a receipt number into the payments search returns every payment. The user reasonably concludes that everything matched — the exact failure the sibling finance module documents in its own source and codes around.

**Fix / open question:** Implement `q` in both services over the free-text columns the placeholders already name, copying the shape at backend/src/modules/finance/finance.service.js:312-319. In `listStructures` (fees.service.js:162-178), after the existing filters: `if (query.q) where[Op.or] = [{ name: { [Op.like]: `%${query.q}%` } }];` — `fee_structures.name` is the only free-text column (models/finance.js:55); `component` is a closed enum already served by its own filter, so leave the placeholder's 'or component' to that control. In `listPayments` (fees.service.js:587-609): `if (query.q) where[Op.or] = [{ receipt_number: ... }, { reference: ... }, { remarks: ... }]` over models/finance.js:186, :193, :204. No schema, column or permission change — `q` is already validated and delivered.

**Evidence:** SRS: docs/SRS-extracted.md:915-932 (§17 / FR-FEE-001, FR-FEE-002) | CODE: frontend/src/app/(school)/school/fees/page.tsx:113-114 and :144, :249-250 and :271; backend/src/modules/fees/fees.validation.js:169, :192 (`listQuery(...)`); backend/src/middlewares/validate.js:157, :167-177; backend/src/modules/fees/fees.controller.js:10, :90; backend/src/modules/fees/fees.service.js:162-178 and :587-609 (no `q` anywhere in the file); backend/src/utils/pagination.js:72-84 (no generic q); contrast at backend/src/modules/finance/finance.service.js:304-318 | CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:667 and :676 (the Fees row), no note

---

### 24 — §20.2 FR-HW-001 (SRS-extracted.md:1096-1104); §20.3 FR-ASG-001 (SRS-extracted.md:1105-1113)

The claim is that two checklist rows and two service docblocks still say a download route does not exist and that there is no file-serving anywhere in the application. Verified on both sides. utils/fileResponse.js exists and exports sendStoredFile. homework.routes.js mounts GET /:id/attachment under homework.view (declared above GET /:id so it cannot be swallowed by the id route) and homework.controller.js:69-70 calls sendStoredFile with the row's own attachment_path/school_id. assignments.routes.js mounts GET /submissions/:id/attachment under assignments.view (declared above /submissions/:id for the same reason) and assignments.controller.js:18,87,96 uses sendStoredFile. Endpoint counts are wrong too: the homework router declares five router.<verb> handlers (GET /, POST /, GET /:id/attachment, GET /:id, PATCH /:id) against the row's 'four', and the assignments router declares nine (grep -c '^router\.' = 9) against the row's 'eight'. Both stale docblocks survive verbatim, and each frames the absence as a deliberate deferral to FR-DOC-001, which is the shape a future reader trusts. The checklist already contradicts itself: row FR-SEC-004 at :878 credits utils/fileResponse.js with re-validating on the way back out. The fix is a documentation correction to match code that already exists — no SRS rule is invented, and no schema, permission or table is touched.

**Consequence:** No runtime consequence — both routes work and are permission-, tenant- and self-scoped through each module's own finder. The consequence is on readers: two service headers and two checklist rows assert that this application cannot serve a file at all, so anyone acting on them would either rebuild a route that already exists or carry forward the assumption that stored *_path columns are never read back — which is precisely the assumption Known Issues #26 was about. fileResponse.js:6-7 itself flags the problem ('four modules carry comments saying exactly that').

**Fix / open question:** Edit docs/IMPLEMENTATION_CHECKLIST.md:568 — 'four endpoints' -> 'five endpoints', and replace the 'Not covered: no download route — there is no file-serving anywhere in the application' clause with the shipped route (GET /:id/attachment under homework.view, streamed by utils/fileResponse.js). Edit :569 — 'eight endpoints' -> 'nine endpoints', same replacement naming GET /submissions/:id/attachment under assignments.view. Delete the '## What FR-HW-001 does NOT get' paragraph at backend/src/modules/homework/homework.service.js:30-40 and the '## What FR-ASG-001 does not get' paragraph at backend/src/modules/assignments/assignments.service.js:58-65, replacing each with the route that now exists; the true half of both (the stored path is never returned in a payload — present() reduces it to a name and a boolean) should be kept. Once done, backend/src/utils/fileResponse.js:6-7's 'four modules carry comments saying exactly that' needs its count re-checked as well.

**Evidence:** SRS: docs/SRS-extracted.md:1104 'Expected Outcome: Homework is available to the relevant class/students.'; docs/SRS-extracted.md:1112 '- Teacher reviews the submission.' Stale claims: docs/IMPLEMENTATION_CHECKLIST.md:568 'four endpoints ... **Not covered:** no download route — there is no file-serving anywhere in the application'; :569 'eight endpoints ... **Not covered:** no download route'; backend/src/modules/homework/homework.service.js:32 'There is **no download route**, because there is no file-serving anywhere in this application'; backend/src/modules/assignments/assignments.service.js:60 'There is **no download route**. There is no file-serving anywhere in this application'. Code that refutes them: backend/src/utils/fileResponse.js:1-51; backend/src/modules/homework/homework.routes.js:90-96; backend/src/modules/homework/homework.controller.js:59-71; backend/src/modules/assignments/assignments.routes.js:95-101; backend/src/modules/assignments/assignments.controller.js:18,87,96. Contradicted by docs/IMPLEMENTATION_CHECKLIST.md:878 '`utils/fileResponse.js` re-validates on the way back out'.

---

### 25 — §19.3 Results 'Includes PDF support and Print support.' (SRS-extracted.md:996); FR-EXAM-005 (SRS-extracted.md:1031-1038)

Verified in full. Row 5.4 at IMPLEMENTATION_CHECKLIST.md:805 still reads 'Result cards (FR-EXAM-005) and the seven documents (FR-DOC-001) still need wiring' and carries forward 'Blocks FR-EXAM-005, FR-DOC-001, ... FR-HW-001's attachment and FR-ASG-001's submission. **Four requirements wait on this and nothing else.**' All four have landed. exams.service.js:115 requires renderTable from utils/pdf.js; resultCardPdf() is defined at :1437 and renders the same card payload; exams.controller.js:192-204 branches on req.query.format === REPORT_FORMATS.PDF, calls service.resultCardPdf(card), records the export in the activity trail, sets application/pdf and a dated Content-Disposition. documents.controller.js:43-51 has the identical PDF branch, so FR-DOC-001 is wired too, and the two attachment routes exist (finding 24). Within one document the checklist contradicts itself: :560 says '`?format=pdf` renders that **same payload** through `src/utils/pdf.js`' and :573 says 'all seven render as PDF via `?format=pdf` (Phase 5.4)'. The two exams.service.js docblocks are the more dangerous copy because they read as a considered position, not a stale note: the header at :87-91 says '**This module produces the complete Result Card payload and does not render a PDF**' and lists three missing capabilities as the justification, and the comment at :1385-1388 repeats 'FR-EXAM-005's PDF is not rendered by this module' 49 lines above resultCardPdf(). (The finding said 30 lines and cited :1383-1385; the offsets are slightly off, the substance is exact.) The fix is documentation only.

**Consequence:** No runtime consequence — the PDF renders. The consequence is that the checklist gives two opposite answers to 'is FR-EXAM-005's PDF built?' depending on which row a reader opens (560/573 say yes, 805/586/806 say no), and the exams service's own header tells a maintainer standing inside the module that the capability was deliberately deferred, ~1300 lines above the function that implements it.

**Fix / open question:** Rewrite the note on row 5.4 at docs/IMPLEMENTATION_CHECKLIST.md:805: drop 'Result cards (FR-EXAM-005) and the seven documents (FR-DOC-001) still need wiring' and either delete the carried-forward 'Blocks ... Four requirements wait on this and nothing else' sentence or mark it explicitly as the superseded previous note, since all four (FR-EXAM-005, FR-DOC-001, FR-HW-001's attachment, FR-ASG-001's submission) shipped. In backend/src/modules/exams/exams.service.js, replace the '## FR-EXAM-005, delivered in half' section at :87-107 with what the module now does (resultCardPdf() renders the same payload resultCard() builds), and rewrite the comment at :1385-1388 to keep the surviving true fact — result_card_path stays null because the Buffer is streamed rather than stored — without the 'not rendered by this module' claim.

**Evidence:** SRS: docs/SRS-extracted.md:996 'Includes PDF support and Print support.'; :1036 '- User exports the result as PDF.' Stale claims: docs/IMPLEMENTATION_CHECKLIST.md:805 'Result cards (FR-EXAM-005) and the seven documents (FR-DOC-001) still need wiring' and '**Four requirements wait on this and nothing else.**'; backend/src/modules/exams/exams.service.js:87-91 '## FR-EXAM-005, delivered in half ... does not render a PDF'; :1386-1387 "FR-EXAM-005's PDF is not rendered by this module". Code that refutes them: backend/src/modules/exams/exams.service.js:115 `const { renderTable } = require('../../utils/pdf');`; :1437 `function resultCardPdf(card) {`; :1446 `return renderTable({ title: 'Result Card', ...`; backend/src/modules/exams/exams.controller.js:192-204; backend/src/modules/documents/documents.controller.js:43-51. Contradicted inside the same doc by docs/IMPLEMENTATION_CHECKLIST.md:560 and :573.

---

### 26 — FR-EXAM-005 — Result Export (PDF / Print), Actor / Role 'Principal / School Admin / Teacher / Parent / Student' (SRS-extracted.md:1031-1038)

Confirmed on every leg. FR-EXAM-005's behaviour is 'User exports the result as PDF' / 'User prints the result' (SRS:1036-1037) and its actor list includes Parent and Student (SRS:1033). The checklist row for that FR closes with '`GET /exams/my-results` reaches this FR's Parent and Student actors with `results.self.view`', offered as this FR's actor coverage. That route cannot produce the FR's artefact: exams.controller.js:210-214 `myResults` calls ApiResponse.paginated and never inspects req.query.format, and its query schema (exams.validation.js:324-330, listQuery over school_id/exam_id/student_id) declares no format key, so ?format=pdf would be stripped even if the controller looked. The only PDF branch is on GET /exams/results/:id (exams.controller.js:192-204), mounted at exams.routes.js:165-170 under requirePermission('results.view') — a strict all-of guard (authorize.js:154-156), with no self-scoped fallback on that route. The seeded catalogue gives ROLES.STUDENT (permissions.js:388) and ROLES.PARENT (:406) 'results.self.view' and not 'results.view' (granted only at :283 and :306). So the two actors the row names are exactly the two who cannot obtain a result-card PDF. The sideways path the finding names checks out too: documents.controller.js:43-51 serves ?format=pdf under documents.view, which Student/Parent do hold (permissions.js:397,411), but only after a documents.generate holder (:338,:356 — not Student/Parent) creates the row, which is FR-DOC-001's requirement. The row is therefore an overstatement of coverage; correcting it is a factual documentation edit that invents nothing.

**Consequence:** A Parent or Student cannot obtain the artefact FR-EXAM-005 is about: GET /exams/results/:id?format=pdf is refused for them (they lack results.view) and GET /exams/my-results answers paginated JSON whatever format is passed. They can read their own/child's published results, and can download a result-card PDF only through §20.5's documents route after a staff member has generated the document. A reader of row 560 would conclude the FR's Parent/Student actor coverage is delivered, and would not look for the gap.

**Fix / open question:** Replace the final sentence of row FR-EXAM-005 at docs/IMPLEMENTATION_CHECKLIST.md:560 with what is true: GET /exams/my-results reaches Parent and Student for *viewing* their own/child's published results under results.self.view, and the PDF/Print half of FR-EXAM-005 does not reach them, because the only PDF route (GET /exams/results/:id) is gated on results.view, which the fixed catalogue does not grant those two roles. If the capability itself is to be closed rather than recorded, the in-repo pattern already exists and needs no new permission: change exams.routes.js:165-170 to requireAnyPermission('results.view', 'results.self.view') — the shape authorize.js:161-163 documents for exactly this case — and have findResult()/resultCard() narrow a self-scoped caller with the rule myResults() already applies (exams.service.js:1512-1560: own student row plus linked children, is_published: true). That is an implementation choice, not a new requirement, but it is a larger change than the row edit and should be decided explicitly.

**Evidence:** SRS: docs/SRS-extracted.md:1033 'Actor / Role: Principal / School Admin / Teacher / Parent / Student'; :1036-1037 '- User exports the result as PDF. - User prints the result.' Claim: docs/IMPLEMENTATION_CHECKLIST.md:560 '`GET /exams/my-results` reaches this FR's Parent and Student actors with `results.self.view`'. Code: backend/src/modules/exams/exams.controller.js:210-214 `async function myResults(req, res) { const pagination = getPagination(req); const result = await service.myResults(...); return ApiResponse.paginated(res, result, pagination); }` — no format branch; backend/src/modules/exams/exams.validation.js:324-330 `const myResults = listQuery(Joi.object({ school_id, exam_id, student_id }))` — no format key; backend/src/modules/exams/exams.routes.js:165-170 `router.get('/results/:id', requirePermission('results.view'), ...)` vs :125-129 `'/my-results', requirePermission('results.self.view')`; backend/src/config/permissions.js:120,122 (the two distinct keys), :283,:306 (results.view holders), :388,:406 (STUDENT and PARENT hold only results.self.view); backend/src/middlewares/authorize.js:154-156 (requirePermission is all-of).

---

### 27 — §33 School MVP screens 'Exams' / 'Timetable' / 'Homework' (SRS-extracted.md:1609, 1611, 1612), serving FR-EXAM-001 (:999), FR-TT-001 (:1082) and FR-HW-001 (:1096)

Every factual leg checks out from the filesystem. The three screens each render a permission-gated 'Add' link to a route that does not exist: exams/page.tsx:325 href="/school/exams/new", timetable/page.tsx:325 href="/school/timetable/new", homework/page.tsx:352 href="/school/homework/new"; `find frontend/src/app -type d -name new` returns nothing and no page.tsx anywhere matches */new/page.tsx. None of the three pages contains a <form>, a POST or a mutation, so there is no inline create either. Eleven such links exist under (school) (the nine */new hrefs plus library/books/new and fees/structures/new), matching the finding's count. `find frontend/src -name 'not-found*'` returns exactly one file, frontend/src/app/(platform)/not-found.tsx; there is none in (school) and none at frontend/src/app/not-found.tsx (the app root holds only layout.tsx, globals.css and the six route groups). That file's copy is platform-specific — its heading text at :30-31 says 'The list screens for the platform surface are in place' and its only link points to /super-admin — and its own docblock at :6-17 scopes itself to 'every unmatched route under this group'. The checklist sentence at :662 ('a group-level not-found.tsx answers those honestly rather than with a bare 404') sits inside row 4.3's platform narrative, where it is defensible; the defect is that row 4.4 at :667 says 'Completed' / 'All 17 built' and its whole narrative (:667-703) contains no occurrence of 'not-found', '/new', '404' or 'dead', so the school surface's eleven dead links are recorded nowhere. IMPLEMENTATION_PROGRESS.md:4247-4256 confirms the recorded count is the platform's seven ('The seven links that answered 404' — and there are exactly seven distinct /new targets under (platform)), predating the school screens. Row 4.3 at :625 establishes that this project treats a dead nav link as a defect ('**The link was previously dead** — ... returned a bare 404, found by running the app'), so this is a recurrence of a defect class already fixed once.

**Consequence:** A Principal or Teacher clicking 'Add exam', 'Add timetable entry' or 'Add homework' navigates to a URL with no matching route and no not-found boundary on the school surface, so the App Router falls back to Next's built-in 404 ('This page could not be found') — the exact outcome the platform not-found page was written to prevent, and the same one row 4.3 records fixing once. Eleven buttons under (school) behave this way. Row 4.4's 'Completed' offers a reader no warning.

**Fix / open question:** Two edits, neither of which needs an SRS rule the source does not give. (1) docs/IMPLEMENTATION_CHECKLIST.md — add to row 4.4's narrative what row 4.3 already says for the platform: every school list screen carries a permission-gated 'Add …' button pointing at a /new route that does not exist (eleven of them), and the school surface has no not-found page, so those links answer with Next's default 404; and scope the sentence at :662 explicitly to the platform group, since (platform)/not-found.tsx covers only that group's unmatched addresses. (2) Optionally mirror the existing precedent in code by adding frontend/src/app/(school)/not-found.tsx with school-appropriate copy and a link to /school — note that only a root frontend/src/app/not-found.tsx is consulted for URLs that match no route at all, so if the intent is to catch the dead links rather than notFound() calls, the root file is the one to add. Neither touches the schema, the catalogue or the 64 tables. Building the create forms is the larger alternative and is already recorded as 'the next increment'.

**Evidence:** SRS: docs/SRS-extracted.md:1609 '- Exams', :1611 '- Timetable', :1612 '- Homework'. Code: frontend/src/app/(school)/school/exams/page.tsx:325 `href="/school/exams/new"`; frontend/src/app/(school)/school/timetable/page.tsx:325 `href="/school/timetable/new"`; frontend/src/app/(school)/school/homework/page.tsx:352 `href="/school/homework/new"`. Absence: no directory named `new` under frontend/src/app; frontend/src/app/ contains only (auth) (parent) (platform) (school) (student) (teacher) layout.tsx globals.css — no not-found.tsx; the only not-found in the tree is frontend/src/app/(platform)/not-found.tsx, whose body at :30-31 reads 'The list screens for the platform surface are in place' and whose link at :34 is `href="/super-admin"`, and whose docblock at :16-17 says 'One file covers every unmatched route under **this group**'. Checklist: docs/IMPLEMENTATION_CHECKLIST.md:662 (inside row 4.3's platform narrative) and :667 '| 4.4 | School: 17 MVP screens | §33 | Completed |' with no dead-link note in :667-703. Precedent: docs/IMPLEMENTATION_CHECKLIST.md:625; IMPLEMENTATION_PROGRESS.md:4247-4251 'The seven links that answered 404 ... Seven dead links, inherited from the exemplar'.

---

### 28 — §22 Export formats — SRS-extracted.md:1183 ("- PDF"); FR-REPORT-002 — SRS-extracted.md:1200 ("User exports the report as PDF.")

The claim is that two checklist rows still record the PDF export as unbuilt/refused while the code ships it. Confirmed on both sides. Code: `SUPPORTED_FORMATS` is frozen as `[JSON, EXCEL, PDF]` (reports.validation.js:37-41); the controller's `EXPORTS` map has a PDF entry with `application/pdf` and a dated `.pdf` filename (reports.controller.js:22,26-29,31-35) and sends the Buffer with `Content-Disposition: attachment` (reports.controller.js:64-66); `toPdf()` renders through `renderTable()` from `src/utils/pdf.js` (reports.service.js:491-506, require at :77); `src/utils/pdf.js` exists (13 KB, header names §22 report export). The suite asserts pdf is accepted and that the supported set is exactly json/excel/pdf (verify-reports.js:157,160-161). Checklist: row FR-REPORT-002's Notes still say PDF "needs a table engine written from pdfkit primitives — row 5.4" and that PDF and Print are "**refused with 422**" (IMPLEMENTATION_CHECKLIST.md:586), and row 5.5 repeats "PDF and print are refused 422 rather than downgraded" (:806) — while the same row 586's Status cell reads "Completed (PDF + Excel)" and row 5.4 (:805) says "§22's seven reports all accept `?format=pdf`". The document contradicts itself and the code in three places. The same staleness is in a source comment: reports.validation.js:25 still reads "Of the three §22 does name, only `excel` is accepted today" ten lines above the constant that lists PDF. Only the Print half of those sentences is still true. Nothing needs inventing to correct them — the code states the answer.

**Consequence:** Documentation-only, but real for a reader: anyone working from row FR-REPORT-002's Notes or row 5.5 concludes that a delivered half of FR-REPORT-002 is still outstanding, and would either re-implement `toPdf()` or report PDF export as a gap. The running system is unaffected — `?format=pdf` returns a PDF today.

**Fix / open question:** In IMPLEMENTATION_CHECKLIST.md:586 replace "**PDF** needs a table engine written from pdfkit primitives — row 5.4" and "Both are **refused with 422**" with PDF shipped in 5.4 via `src/utils/pdf.js` and only `print` refused 422; in :806 change "PDF and print are refused 422" to "print is refused 422; PDF shipped in row 5.4". In backend/src/modules/reports/reports.validation.js:25 replace "only `excel` is accepted today" with "`excel` and `pdf` are accepted; `print` is not". No SRS interpretation is required — the code is the authority for what ships.

**Evidence:** SRS: docs/SRS-extracted.md:1183, :1200 | Stale docs: docs/IMPLEMENTATION_CHECKLIST.md:586, :806; backend/src/modules/reports/reports.validation.js:25 | Code that contradicts them: backend/src/modules/reports/reports.validation.js:37-41; backend/src/modules/reports/reports.controller.js:26-29, :64-66; backend/src/modules/reports/reports.service.js:491-506; backend/src/utils/pdf.js:1-30; backend/scripts/verify-reports.js:157, :160-161

---

### 29 — §22 Export formats — SRS-extracted.md:1185 ("- Print"); FR-REPORT-002 Functional Behavior — SRS-extracted.md:1202 ("- User prints the report.")

Confirmed on every leg. Server: `print` is absent from `SUPPORTED_FORMATS` (reports.validation.js:37-41), so `format=print` fails Joi and is refused 422 — asserted twice (verify-reports.js:158-159 at the schema level, :839-842 over HTTP). Client: I grepped the whole of frontend/src for `window.print`, `@media print`, `onafterprint` and `print()` across .tsx/.ts/.css — zero matches; the only `print` occurrences are 18 prose uses of the word inside comments (e.g. "printing `class_id: 7` would put a number in front of an…"). There is exactly one stylesheet, frontend/src/app/globals.css, and it has no print block. So the checklist's justification — "there is no view engine in this application, so the JSON report is what a client prints" (:586) — defers to a client that implements no printing. The constants file additionally documents behaviour that does not exist: constants.js:660 "`print` returns a print-ready payload/HTML", which reports.service.js:64-67 identifies as a promise "nothing here can produce" and leaves in place; `REPORT_FORMATS.PRINT` has exactly one reader in the entire backend, the assertion that refuses it (grep for PRINT over src/ and scripts/ returns constants.js:665 and verify-reports.js:159 only). §19.3's result card took the identical deferral (checklist:560, "`print` is refused for §22's reason"), so the same behaviour is missing in two places. I considered 'real-blocked' — §22 lists Print as an "export format" beside PDF and Excel, and the SRS never says whether print is a server-produced shape or a browser print — but the fix does not depend on settling that: FR-REPORT-002 phrases the behaviour as an actor's action ("User prints the report"), which a print control over the rendered report satisfies with nothing invented. Only the server-format reading would need a decision the SRS does not supply.

**Consequence:** A user cannot print a report anywhere in the product. Asking the API for it returns 422; the one screen that renders a report offers no print control and the app has no print stylesheet, so a browser Ctrl+P prints the full application chrome and navigation rather than a report. One of §22's three named export formats is delivered by neither tier, and the recorded justification for that points at a client that does not do it.

**Fix / open question:** Two parts, neither inventing a rule. (1) Delete the false clause in backend/src/config/constants.js:660 — `print` returns nothing; the comment should say it is client-side and not a server format. (2) Give §33's Super Admin Reports screen (frontend/src/app/(platform)/super-admin/reports/page.tsx) a print control that calls `window.print()` plus a `@media print` block in frontend/src/app/globals.css that hides nav/chrome and prints the rendered report, so "User prints the report" (SRS:1202) is satisfied by the actor-phrased behaviour the FR states. Leave `format=print` refused 422 and correct docs/IMPLEMENTATION_CHECKLIST.md:586 and :560 to say print is client-side and now implemented rather than merely deferred. Note the scope limit: only reports that are actually rendered can be printed, so this covers the Subscription report today and the other six only once finding 30/31's screen work lands. If instead someone reads §22's "Export formats" list as requiring `?format=print` to return bytes, that half is blocked — the SRS never says what shape a print response has, and answering it would mean inventing a view layer.

**Evidence:** SRS: docs/SRS-extracted.md:1185, :1202 | Server refusal: backend/src/modules/reports/reports.validation.js:37-41; backend/scripts/verify-reports.js:158-159, :839-842 | False comment: backend/src/config/constants.js:660 (flagged but unfixed at backend/src/modules/reports/reports.service.js:64-67) | Client absence: grep of frontend/src for `window.print|@media print|print()` over *.tsx/*.ts/*.css → no matches; frontend/src/app/globals.css is the only stylesheet and has no print block | Deferral text: docs/IMPLEMENTATION_CHECKLIST.md:586, and the same position at :560

---

### 30 — FR-REPORT-001 Actor / Role — SRS-extracted.md:1190 ("Super Admin / Principal / School Admin / Accountant / Teacher"); §33 Super Admin "Reports" — SRS-extracted.md:1594

The checklist's assertion — "Six of the seven resolve a school before they can count anything, so a platform caller cannot run them at all… Rendering all seven as links would have claimed a Super Admin can run reports they structurally cannot" (IMPLEMENTATION_CHECKLIST.md:656) — is false, and I confirmed every link in the chain. (a) A Super Admin's tenant is `{isPlatform: true, organizationId: null, schoolId: null}` (resolveTenant.js:108-117). (b) `buildModuleGuard` short-circuits before any snapshot loads: `if (req.tenant.isPlatform) return next();` (entitlement.js:256), so `requireModule(MODULES.REPORTS, …)` on all six school routes (reports.routes.js, the `SCHOOL_REPORTS` loop) never refuses a platform caller. (c) Both `requirePermission` calls pass: `DEFAULT_ROLE_PERMISSIONS[SUPER_ADMIN] = ALL` (permissions.js:254). (d) All six schemas accept `school_id` — the four local ones via `shared.school_id` (reports.validation.js:44) and the two delegated ones because §16's and §18's own report schemas declare it (attendance.validation.js:117; finance.validation.js:243-244), and reports.validation.js:78 and :98 reuse those objects verbatim. (e) `schoolOf()` calls `resolveSchool(req, query.school_id)` (reports.service.js:126-134); for a caller with no `tenant.schoolId` it requires `school_id` and then honours it — `findByPk(requested)` — and the org-mismatch and `TENANT_SCOPE_REQUIRED` branches below it are both skipped for a platform caller (schoolScope.js:45-68). So `GET /reports/students?school_id=N` as a Super Admin returns 200. The refusal code named in the frontend header is wrong too: `SCHOOL_CONTEXT_REQUIRED` is raised only inside `resolveGatedSchoolId` (entitlement.js:100-105), which is reached from `loadSnapshot` *after* the `isPlatform` short-circuit, so a platform caller can never see it; `resolveSchool` raises a 422 validation error instead (schoolScope.js:46-50). The premise is false and it is load-bearing, not decorative: the screen renders the six as inert `<li>` cards on the strength of it (page.tsx:58-65, 180-187).

**Consequence:** FR-REPORT-001's first-named actor cannot reach six of its seven reports through the product, even though the API serves all six to that actor. The Super Admin Reports screen shows Students / Attendance / Fees / Expenses / Exams / Teachers as descriptive cards with no link and no action, and defers them to "a school's own Reports screen" (page.tsx:19-20) — a screen that does not exist and that §33's School list (SRS:1598-1614) does not name, so there is no Reports surface anywhere in the frontend for those six. Anyone reading checklist:656 also learns a false fact about the entitlement middleware.

**Fix / open question:** Correct the false premise first: IMPLEMENTATION_CHECKLIST.md:656 and the page header comment at frontend/src/app/(platform)/super-admin/reports/page.tsx:11-23 must say that a platform caller runs the six by naming `?school_id=N` (entitlement.js:256 skips the module gate; schoolScope.js:45-53 requires then honours the id), and that the refusal for a missing id is a 422 validation error, not `SCHOOL_CONTEXT_REQUIRED`. Then make the six runnable on the §33-named screen: add a school selector fed by the existing `GET /schools` the Schools screen already uses, and pass its id as `school_id` to the six endpoints, replacing the static `<li>` cards at page.tsx:180-187. Both parts follow from the code and from FR-REPORT-001's actor list — no new endpoint, table, permission or rule.

**Evidence:** SRS: docs/SRS-extracted.md:1190, :1594 | False claim: docs/IMPLEMENTATION_CHECKLIST.md:656; frontend/src/app/(platform)/super-admin/reports/page.tsx:11-13, :22-23 | Code: backend/src/middlewares/resolveTenant.js:108-117; backend/src/middlewares/entitlement.js:256 (and :100-105 for the code that is never reached); backend/src/config/permissions.js:254; backend/src/utils/schoolScope.js:45-68; backend/src/modules/reports/reports.service.js:126-134; backend/src/modules/reports/reports.validation.js:44, :78, :98; backend/src/modules/attendance/attendance.validation.js:116-117; backend/src/modules/finance/finance.validation.js:243-244

---

### 31 — FR-REPORT-002 Functional Behavior — SRS-extracted.md:1200-1202; §33 Super Admin "Reports" — SRS-extracted.md:1594

Confirmed by reading the whole file (191 lines). The screen issues one call, `api.get<SubscriptionReport>('/reports/subscriptions', { signal })` (page.tsx:84-86), with no `format` in the query; the remaining markup is a heading, a state-badge row, a `DataTable` of plans (page.tsx:150-169) and six static `<li>` cards (page.tsx:180-187). There is no download control, no format selector, no print control, and no export string anywhere: a grep of frontend/src for `format=excel|format=pdf|'excel'|download|Blob|createObjectURL` returns exactly one hit, an unrelated comment in the Documents screen. `lib/apiClient.ts` reinforces it — `request()` always ends in `await response.json()` (apiClient.ts:319) and has no blob/binary path, so no screen could export today even if it wanted to. Meanwhile the server serves the exports the FR names: `EXPORTS` carries Excel and PDF with the right MIME and a dated `Content-Disposition: attachment` (reports.controller.js:26-29, 64-66), and Super Admin holds `reports.export` (permissions.js:254, `ALL`) which the conditional `exportGuard` checks only when a format is asked for (reports.routes.js). So FR-REPORT-002's two file formats are reachable only by hand-crafting a URL — and not even that from the browser, since the access token lives in memory and is sent as an `Authorization` header (apiClient.ts:274), so a pasted URL is unauthenticated. Checklist row 4.3 records the screens as "All sixteen exist and are reachable — fifteen functional" (:625) with no qualification of the Reports screen, which runs 1 of §22's 7 reports and offers 0 of its 3 export formats.

**Consequence:** A Super Admin — one of FR-REPORT-002's named actors — cannot export a report at all from the shipped product. The Subscriptions report can be read on screen and nothing else: no Excel, no PDF, no print. Because the token is header-borne, the documented workaround of typing `?format=excel` into the address bar does not work either, so the Excel and PDF exports built in §2z and Phase 5.4 have zero reachable callers in the UI. The checklist counts this screen among the "fifteen functional" without recording that it delivers one of seven reports and none of three export formats.

**Fix / open question:** Add a binary-capable helper beside `request()` in frontend/src/lib/apiClient.ts (same bearer header and 401-refresh path, but `response.blob()` instead of `response.json()`), then put "Export Excel" and "Export PDF" buttons on frontend/src/app/(platform)/super-admin/reports/page.tsx that call `/reports/subscriptions?format=excel|pdf`, gated on `can('reports.export')` exactly as the existing `can('reports.subscription.view')` check at page.tsx:138 gates the read, and hand the blob to the user via an object URL using the server's `Content-Disposition` filename (reports.controller.js:31-35). Extend the same two controls to the six school reports once finding 30's school selector exists. Print is the third format and is covered by finding 29. Qualify docs/IMPLEMENTATION_CHECKLIST.md:625 to say the Reports screen runs one of seven reports. Nothing here needs a new endpoint, permission or rule — the API, the permission and the MIME types already exist.

**Evidence:** SRS: docs/SRS-extracted.md:1200-1202, :1594 | Screen: frontend/src/app/(platform)/super-admin/reports/page.tsx:84-86 (no `format`), :128-189 (entire render — no export/print control), :180-187 (six static cards) | No client export path: grep of frontend/src for `format=excel|format=pdf|download|Blob|createObjectURL` → one unrelated comment (school/documents/page.tsx:20); frontend/src/lib/apiClient.ts:319 always parses JSON; :274 sends the bearer token as a header | Server side that is unused: backend/src/modules/reports/reports.controller.js:26-29, :64-66; backend/src/config/permissions.js:254 | Unqualified row: docs/IMPLEMENTATION_CHECKLIST.md:625

---

### 32 — §25 / FR-PERF-001 — SRS-extracted.md:1301-1306, :1319

The finding is a claim about checklist row 5.8, and every one of its sub-claims checks out against the tree. Row 5.8 (IMPLEMENTATION_CHECKLIST.md:809) asserts three things: that `src/config/queue.js` "still has **no consumer**"; that `src/jobs/handlers/` and `src/jobs/worker.js` "do not exist"; and, in its closing sentence, that "What remains is the handlers and the worker process". All three are false now. `backend/src/jobs/handlers/index.js` exists (108 lines), requires `registerHandler` from `../../config/queue` at :41 and registers four of the eight JOB_NAMES at :56-82 via `registerAll()` at :100-106. `backend/src/jobs/worker.js` exists (111 lines) and calls `runNow(name, payload)` at :101. `backend/src/app.js:164` requires the module and :242 calls `registerJobHandlers()` from inside `createApp()` (defined at :232), with a comment at :235-241 explaining the placement — so the FIFO in `config/queue.js` (which self-drains via `setImmediate(() => this.drain())` at :42) does have registered handlers as of app construction, i.e. a consumer. Two live producers exist: `auth.service.js:549` and :729. The self-contradiction is real too: row 7.8 at IMPLEMENTATION_CHECKLIST.md:906 says "`npm run worker` runs one job on demand" and points "See 5.8" — the row it contradicts. Drift attribution: handlers/index.js and worker.js are both mtime 2026-09-04 14:58, while IMPLEMENTATION_CHECKLIST.md is 2026-09-05 12:14 — the file was edited a day after the code landed (row 885 even carries a fresh §36 correction), so row 5.8 was simply left behind rather than being written before the code. Fixing it is pure transcription of what the code does; nothing has to be invented, and no SRS decision is needed.

**Consequence:** A reader planning work from row 5.8 is told two files that exist (219 lines between them, wired into `createApp()`) are outstanding and would rebuild them; and is told the queue is unconsumed when four handlers are registered at app construction and two producers already enqueue. Because 5.8 is the row §25's queue evidence hangs on, it also makes FR-PERF-001's queue half look less complete than it is — the opposite direction from this project's usual drift, and the checklist states the two positions ninety-seven lines apart (809 vs 906) without noticing.

**Fix / open question:** Rewrite row 5.8's note to the observed state: `src/jobs/handlers/index.js` registers four of the eight `JOB_NAMES` (send_email, send_notification, sync_usage, database_backup) and `app.js:242` calls `registerAll()` inside `createApp()`, so the FIFO has a consumer; `src/jobs/worker.js` exists and runs one job inline via `runNow()`. Delete the sentence 'What remains is the handlers and the worker process'. Keep the §29/§35 no-jobs-table point and the cron-reconciliation durability argument, which are still accurate, and — per finding 35 — add the disclosure that four job names are deliberately unregistered.

**Evidence:** SRS: docs/SRS-extracted.md:1305-1306 '- Background jobs' / '- Queue system'; docs/SRS-extracted.md:1319 '- System processes heavy reports and long-running tasks via Background jobs and a Queue system.' || FALSE CHECKLIST TEXT: docs/IMPLEMENTATION_CHECKLIST.md:809 '...and still has **no consumer**; `src/jobs/handlers/` and `src/jobs/worker.js`, which it and `package.json` both name, do not exist. ... What remains is the handlers and the worker process' || CODE: backend/src/jobs/handlers/index.js:41 `const { registerHandler } = require('../../config/queue');`, :56-82 (the four-entry HANDLERS map), :100-106 `function registerAll() { ... registerHandler(name, handler) ... }`; backend/src/jobs/worker.js:101 `runNow(name, payload)`; backend/src/app.js:164 `const { registerAll: registerJobHandlers } = require('./jobs/handlers');` and :242 `registerJobHandlers();` inside `function createApp()` at :232; backend/src/modules/auth/auth.service.js:549 and :729 `enqueue(JOB_NAMES.SEND_EMAIL, ...)` (grep for `enqueue(` across backend/src returns only these two call sites plus comments) || SELF-CONTRADICTION: docs/IMPLEMENTATION_CHECKLIST.md:906 (row 7.8) '`npm run worker` runs one job on demand'

---

### 34 — §24 / §25 — the Phase 6 preamble governing every §24 and §25 test row (docs/IMPLEMENTATION_CHECKLIST.md:817-820); SRS-extracted.md:1293-1320 (§25) is what row 6.15 reports against

Every factual claim in the preamble is false against the current tree. (1) 'There is no jest suite' — `backend/tests/verify.test.js` exists (10,521 bytes) and `package.json:27` defines `"test": "jest --runInBand"`. (2) '`tests/` does not exist' — it holds baseline.json, globalSetup.js, helpers/, setup.js, verify.test.js and a written .last-run.json. (3) '`tests/setup.js` is referenced by the jest config in `package.json` but absent' — the reference is real (package.json:69-71 `"setupFilesAfterEnv": ["<rootDir>/tests/setup.js"]`, inside a full jest block at :66-79 that also wires `globalSetup` at :68), and the file is present at 2,778 bytes. (4) 'the `msms_test` database has not been created' — verified indirectly but firmly without running anything: `tests/globalSetup.js:46-70` refuses to proceed unless `SELECT DATABASE()` literally returns `msms_test`, and `tests/.last-run.json` records a completed run (`"database":"msms_test"`, `"durationMs":121389`) with all 38 suites at `"exitCode":0`; that file could not exist in that shape against an absent database. (5) '`npm test` fails' — contradicted by row 6.17 at :840 and by the same .last-run.json. And the count: 'the thirteen standalone `scripts/verify-*.js` suites' — `ls backend/scripts/verify-*.js` returns 38, matching `tests/baseline.json`'s `"suiteCount": 38`. One correction to the finding's own evidence, which does not change the verdict: it quotes row 6.17 as '5,329 jest tests' but the row now reads 5,338 — the checklist was edited (mtime 2026-09-05 12:14, later than tests/verify.test.js at 2026-09-05 06:57 and package.json at 06:47) without the preamble being touched, which is also the drift attribution. Fixing this is transcription of the tree plus the checklist's own row 6.17; no requirement or value is invented.

**Consequence:** The paragraph is positioned as the reading instruction for the whole of Phase 6, so a reader takes every 'Tested' below it — including the §24 security rows 6.3-6.9 and the §25 performance row 6.15 — as evidence from hand-run scripts only, and believes `npm test` fails and the test database is missing. The tree contains the opposite: 38 suites run through `tests/globalSetup.js` under `npm test` against `msms_test`, checked against an exact per-suite assertion baseline. Twenty-one lines below the preamble, row 6.17 states that outcome directly, so the document tells a reader two incompatible things about the same command on the same screen.

**Fix / open question:** Replace the preamble with what row 6.17 and the tree already establish: `tests/` exists (setup.js, globalSetup.js, helpers/, verify.test.js, baseline.json), `package.json`'s jest block wires them, `msms_test` exists and `globalSetup.js` refuses to run against anything else, and `npm test` runs the 38 `scripts/verify-*.js` suites as child processes with 0 failures. Carry forward row 6.17's own qualification rather than dropping it — jest reports the suites, it does not execute them; the assertions run in `globalSetup` before jest evaluates its first test file — so the replacement does not swing from understating to overstating. Change 'thirteen' to 38 wherever it survives, and drop the 'run by hand' framing since the entry point is now `npm test`.

**Evidence:** PREAMBLE: docs/IMPLEMENTATION_CHECKLIST.md:817-820 '**There is no jest suite.** `tests/` does not exist, `tests/setup.js` is referenced by the jest config in `package.json` but absent, the `msms_test` database has not been created, and `npm test` fails. Every status below refers to the thirteen standalone `scripts/verify-*.js` suites, which are run by hand.' || REALITY: `ls backend/tests/` -> .last-run.json, baseline.json, globalSetup.js, helpers/, setup.js (2,778 bytes), verify.test.js (10,521 bytes); backend/package.json:27 `"test": "jest --runInBand"` and :66-79 the jest block (`"globalSetup": "<rootDir>/tests/globalSetup.js"` at :68, `"setupFilesAfterEnv": ["<rootDir>/tests/setup.js"]` at :69-71); backend/tests/globalSetup.js:46-58 `async function assertTargetDatabase()` ... `if (actual !== EXPECTED_DATABASE) throw new Error('Refusing to run: connected to database ...')`; backend/tests/.last-run.json `"database":"msms_test"` with 38 results at exitCode 0; backend/tests/baseline.json `"suiteCount": 38`, `"totalAssertions": 5142`; `ls backend/scripts/verify-*.js` -> 38 files || SELF-CONTRADICTION: docs/IMPLEMENTATION_CHECKLIST.md:840 (row 6.17) '**Done. `npm test` runs 5,338 jest tests, 0 failures, exit 0**' || GOVERNED ROWS: docs/IMPLEMENTATION_CHECKLIST.md:826-832 (rows 6.3-6.9, §24) and :838 (row 6.15, §25 pagination/performance)

---

### 35 — §25 / FR-PERF-001 — SRS-extracted.md:1298 ('- Heavy reports') and :1319

The claim holds on both sides. SRS-extracted.md:1319 is a Functional Behavior bullet of FR-PERF-001, not a topic list: 'System processes heavy reports and long-running tasks via Background jobs and a Queue system' — heavy reports are the workload the clause names, and :1298 lists them again. The code declines that specific workload deliberately and says so: `backend/src/jobs/handlers/index.js:85-90` is an `UNREGISTERED` map whose first entry is `[JOB_NAMES.GENERATE_REPORT]: 'renders a Buffer with no storage or serving route to complete into'`, with `GENERATE_DOCUMENT` (§20.5) next for the same reason; the HANDLERS map at :56-82 registers four names and generate_report is not among them. §22 report rendering is synchronous inside the request: `backend/src/modules/reports/reports.controller.js:42` opens `async function reportHandler(req, res)`, :55 `const buffer = await service[spec.render](report);` and :65 `return res.status(200).send(Buffer.from(buffer));` — no enqueue anywhere on that path. A grep for `enqueue(` across backend/src returns exactly two call sites, both `JOB_NAMES.SEND_EMAIL` in auth.service.js (:549, :729); the rest are comments. So the checklist row's unqualified 'background jobs and the queue system' at IMPLEMENTATION_CHECKLIST.md:881 does assert the FR complete while the one workload §25 names by name is excluded by design, and the row discloses none of it. I checked whether the exclusion is disclosed elsewhere in the checklist before confirming: grep for 'generate_report' / 'unregistered' across IMPLEMENTATION_CHECKLIST.md returns zero hits — the disclosure exists only in IMPLEMENTATION_PROGRESS.md (:3716 and :8594), not in the document the row lives in, and row 5.8 (the queue row a reader would follow) is stale in the opposite direction (finding 32). The verdict is real-fixable because the defect being reported is the checklist's silence, and writing down what the code does invents nothing. Closing the underlying code gap is a separate matter and is blocked — IMPLEMENTATION_PROGRESS.md:8594-8597 records that generate_report would need somewhere to persist a rendered Buffer, 'a separate decision nobody has made', and the SRS supplies no storage profile; §29/§35 bar a new table or column for it. My verdict should not be read as licence to register the handler.

**Consequence:** Anyone auditing §25 from the checklist reads FR-PERF-001 as Completed with 'background jobs and the queue system' covering the clause, and has no way to learn from that document that heavy reports — the workload the clause names — never touch the queue, that `generate_report` and `generate_document` are deliberately unregistered, and that the only two live producers in the application are password-reset and verification e-mails. The row a reader would follow next for detail, 5.8, is stale in the opposite direction. Nothing in the running system misbehaves: reports render and download correctly; the damage is entirely to the accuracy of the §25 completion claim.

**Fix / open question:** Amend row 881 to disclose what the code already states in `jobs/handlers/index.js:85-90`: four of the eight `JOB_NAMES` are registered; `generate_report` and `generate_document` are deliberately unregistered because a rendered Buffer has no storage profile or serving route to complete into; §22 reports render synchronously in the request handler; and the only enqueue call sites are the two SEND_EMAIL calls in auth.service.js. That makes SRS-extracted.md:1319's heavy-reports clause visibly unmet for the named workload, so the status word should follow the disclosure — the checklist already uses a qualified form for exactly this ('In Progress — **everything but the round trip**', row 5.2) and no new vocabulary is needed. Do NOT attempt the code half as part of this: registering `generate_report` requires deciding where a generated artefact is persisted and how it is served, which the SRS does not specify and which §29/§35 forbid solving with a new table or column.

**Evidence:** SRS: docs/SRS-extracted.md:1298 '- Heavy reports'; docs/SRS-extracted.md:1319 '- System processes heavy reports and long-running tasks via Background jobs and a Queue system.' || CHECKLIST (the unqualified claim): docs/IMPLEMENTATION_CHECKLIST.md:881 '| FR-PERF-001 | Performance Optimization Implementation | §25 | Completed | Indexes declared on every model; `utils/pagination.js` on every list endpoint; entitlement caching with a TTL; background jobs and the queue system (§2ab, §2ad). §25 states no numeric targets and none is invented |' || CODE (the exclusion): backend/src/jobs/handlers/index.js:85-90 `const UNREGISTERED = Object.freeze({ [JOB_NAMES.GENERATE_REPORT]: 'renders a Buffer with no storage or serving route to complete into', [JOB_NAMES.GENERATE_DOCUMENT]: ... })`, with HANDLERS at :56-82 covering only send_email/send_notification/sync_usage/database_backup || CODE (synchronous render): backend/src/modules/reports/reports.controller.js:42 `return async function reportHandler(req, res) {`, :55 `const buffer = await service[spec.render](report);`, :65 `return res.status(200).send(Buffer.from(buffer));` || CODE (producers): backend/src/modules/auth/auth.service.js:549, :729 are the only `enqueue()` call sites in backend/src || NOT DISCLOSED IN THIS DOCUMENT: grep 'generate_report|unregistered' docs/IMPLEMENTATION_CHECKLIST.md -> no hits; the reason lives only at IMPLEMENTATION_PROGRESS.md:3716 and :8594-8597

---

### 36 — §5 role table SRS-extracted.md:96 ("global settings (see Section 9)"); §33 Super Admin screen list SRS-extracted.md:1595 ("- Settings")

The claim is that four places assert "no settings permission exists in the fixed catalogue of 109" and use that as the reason the Settings nav entry borrows `schools.view`. Verified: the permission does exist. `settings.platform.manage`, name "Manage global settings" — a near-verbatim match for SRS:96's "global settings" — is entry #14 of the catalogue at permissions.js:39, and super_admin holds it via `[ROLES.SUPER_ADMIN]: ALL` at :254. A ripgrep for `settings\.platform\.manage` across the whole repo returns exactly one hit outside the findings document: its own definition line. So the catalogue carries an orphan key while the screen it was written for gates on a different one.

The four false statements are all present verbatim: IMPLEMENTATION_CHECKLIST.md:625, nav.ts:104, settings/page.tsx:17 (source comment) and settings/page.tsx:64 (rendered to the viewer inside the amber callout). I confirmed nav.ts:109 gates on `schools.view` and that `visibleNav()` (nav.ts:244-259) filters purely on `can(item.permission)`, so the gate is the whole gate.

One correction to the finding's own evidence: `schools.view` is granted to organization_admin at permissions.js:262, not :259 (:259 is a comment line inside that block). The substance is unaffected.

The fix invents nothing. SRS:96 assigns "global settings" to Super Admin and SRS:97 records that Organization Admin workflows are Not Specified; a key already in the fixed 109, already seeded to super_admin alone, already named for exactly that responsibility, replaces a borrowed one. Deleting the false sentence is pure fact correction. §35 is not engaged — no key, table or role is added.

Note: finding 5 (line 79 of the findings doc, another slice) is the same defect with a narrower scope. Triage them together to avoid two fixes to the same four lines.

**Consequence:** Two. (1) Documentary and user-visible: settings/page.tsx:64 renders "no settings permission exists in the catalogue of 109" to the viewer, and it is false. (2) Access: an organization_admin holds `schools.view` (permissions.js:262) and so passes the Settings gate. The leak is narrow — resolveTenant.js:108 sets `isPlatform: true` for super_admin only (PLATFORM_ROLES = [SUPER_ADMIN], constants.js:30), so login sends an organization_admin to `/school` (login/page.tsx:50), and `app/(platform)/` has no layout guard, only `not-found.tsx`. An organization_admin who navigates directly to `/super-admin` therefore renders PLATFORM_NAV with a Settings entry. The page itself holds no data and the backend still refuses platform calls, so the exposure is a nav entry into the platform surface, not data.

**Fix / open question:** Two edits, both fact corrections. (1) Change frontend/src/lib/nav.ts:109 to `permission: 'settings.platform.manage'`. (2) Delete the "no settings permission exists in the fixed catalogue of 109" sentence from all four places (IMPLEMENTATION_CHECKLIST.md:625, nav.ts:104-105, settings/page.tsx:17-18, settings/page.tsx:64) and replace it with the true statement: the catalogue's `settings.platform.manage` gates the screen; what the screen may contain is still unspecified (SRS:96 defers to §9, which defines only 9.1/9.2/9.3). No key, role or grant is added or removed — 109/11/353 are untouched.

**Evidence:** SRS: docs/SRS-extracted.md:96 ("| Super Admin | … platform-wide dashboard and reports, and global settings (see Section 9). |"), docs/SRS-extracted.md:97 (Organization Admin workflows "Not Specified in Source Requirements"), docs/SRS-extracted.md:1595 ("- Settings"). CODE — the permission that is claimed not to exist: backend/src/config/permissions.js:39 "{ key: 'settings.platform.manage', group: 'Platform', name: 'Manage global settings', module: null },"; grant at backend/src/config/permissions.js:254 "[ROLES.SUPER_ADMIN]: ALL,". CODE — the four false claims: docs/IMPLEMENTATION_CHECKLIST.md:625, frontend/src/lib/nav.ts:104, frontend/src/app/(platform)/super-admin/settings/page.tsx:17, and rendered at frontend/src/app/(platform)/super-admin/settings/page.tsx:64. CODE — the borrowed gate and the leak: frontend/src/lib/nav.ts:109 "{ label: 'Settings', href: '/super-admin/settings', permission: 'schools.view' },"; backend/src/config/permissions.js:262 "'schools.view'," inside [ROLES.ORGANIZATION_ADMIN]. CODE — the filter: frontend/src/lib/nav.ts:250 "if (!can(item.permission)) return false;".

---

### 37 — §33 SaaS Engine list SRS-extracted.md:1616-1637 (21 items); §29 Billing table SRS-extracted.md:1445; §35 "Additional workflows" SRS-extracted.md:1668

The citation is false and grep proves it. `grep -n -i "quotation" docs/SRS-extracted.md` returns exactly one line in the whole 1,698-line document: :1445, the bare table name inside §29's Billing group. §33's SaaS Engine list runs 1617-1637 and is exactly the 21 items the finding enumerates — I read all 21 (Trial, Subscription, Renewal, Upgrade, Downgrade, Proration, Grace Period, Expiry, Suspension, Add-ons, Usage Tracking, Overage, Coupons, Discounts, Taxes, Invoices, Payments, Refunds, Custom Pricing, Custom Limits, Feature Overrides). There is no Quotations entry. So quotations.service.js:4's "§33's SaaS-Engine *Quotations* entry", validation.js:4's "§33 *Quotations*", controller.js:4's "§29 / §33", the seven "| §33 |" rows in routes.js:10-16, and the checklist's "| §33 | Refunds, taxes, quotations, … |" at :478 all attribute the module to a clause that does not exist.

The finding's supporting detail also checks out: the service's own header at :8 concedes "§13 gives quotations no functional requirement and no field list", and the module nevertheless implements a five-state lifecycle, seven routes, two catalogue permissions (quotations.view / quotations.manage at permissions.js:71-72) and invoice issuance on accept. The precedent the finding cites is real: roles.validation.js:12 says "§33 does **not** list a 'Roles & Permissions' screen".

The fix as reported — correct the citation — follows from the SRS with nothing invented: §29:1445 is the true and only warrant, and stating "§33 does not list Quotations" is the same sentence roles.validation.js already writes. That is why this is fixable rather than blocked.

Separately, and NOT part of this fix: whether a bare §29 table name authorizes a five-state lifecycle with automatic invoice issuance is a §35:1668 "Additional workflows" question the SRS does not answer. Removing or trimming the module would need that decision. I am not recommending it; I am flagging that the citation fix does not settle it.

**Consequence:** No runtime effect — the endpoints work as written. The consequence is to the audit trail, which matters on a project whose standing rule is that the SRS is the sole source of truth: an auditor tracing seven endpoints and two of the fixed 109 permissions to "§33" finds nothing there, and cannot tell whether the module is warranted. The same mis-attribution is the header of the checklist row that records it Completed.

**Fix / open question:** Replace the §33 attribution in five places with the true one. quotations.service.js:4, quotations.validation.js:4, quotations.controller.js:4 → cite §29:1445 (the table name) alone, and add the roles.validation.js:12 sentence in the same form: "§33's SaaS Engine list does not name Quotations; the only warrant is the §29 table." quotations.routes.js:10-16 → change the seven "| §33 |" cells to "| §29 |". IMPLEMENTATION_CHECKLIST.md:478 → move quotations out of the "§33" row header, or mark it "§29 table only, no §33 entry, no FR". Nothing is added or removed from the schema, the catalogue or the routes.

**Evidence:** SRS: docs/SRS-extracted.md:1616 "## SaaS Engine" through :1637 "- Feature Overrides" is the complete list, Quotations absent; the sole occurrence of the word in the SRS is docs/SRS-extracted.md:1445 "| quotations |"; docs/SRS-extracted.md:1668 "- Additional workflows". CODE: backend/src/modules/quotations/quotations.service.js:4 "Quotations — SRS §29's `quotations` table and §33's SaaS-Engine *Quotations* entry."; backend/src/modules/quotations/quotations.validation.js:4 "Quotation schemas — SRS §29 `quotations` / §33 *Quotations*."; backend/src/modules/quotations/quotations.controller.js:4 "Quotation controllers — SRS §29 / §33."; backend/src/modules/quotations/quotations.routes.js:10-16 (seven rows, each "| §33 |"). The concession: backend/src/modules/quotations/quotations.service.js:8. The precedent: backend/src/modules/roles/roles.validation.js:12. CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:478.

---

### 38 — §29 SRS-extracted.md:1518 ("No tables beyond those listed above are introduced"); §3 SRS-extracted.md:57-77; §28 SRS-extracted.md:1377-1396; §32 step 2 SRS-extracted.md:1565

PRIORITY finding, and it is real — but note precisely what it does and does not claim. It does NOT claim the 64-table rule is violated. It claims the justification for the one deliberate exception cites the wrong sections and puts two phrases in the SRS's mouth that are not in it.

Both halves verified. `grep -n -i "migrat" docs/SRS-extracted.md` returns exactly one line in 1,698: :1565 "2. Create database migration." — §32 Developer Working Method step 2. I read §3 in full (lines 57-77: Node.js, Express.js, REST API, MySQL, Sequelize-or-Prisma, JWT, RBAC, PBAC, then the frontend list and the separation requirement) — no migration item. I read §28 in full (1377-1396) — Swagger/OpenAPI, the seven documented fields, FR-APIDOC-001; no migration item. The quoted strings "Database Migrations" and "Run migrations" appear nowhere in the SRS.

migrator.js:19-22 reads exactly as reported: "`sequelize_meta` is not application data — it is the ledger required by the migration tooling that SRS §3 and §28 themselves mandate (\"Database Migrations\", \"Run migrations\")". IMPLEMENTATION_CHECKLIST.md:133 repeats it as "§29 fixes the schema at 64 tables, but §3/§28 mandate migrations". Four lines above, at :129, the same table cites the correct clause — "`sequelize-cli`-style migrations satisfy Working Method step 2 (\"Create database migration\")" — so the project already knows where the requirement lives; :133 simply names the wrong two sections.

The exception itself is sound and I am not disturbing it: §32:1565 does require creating database migrations, migrations need a ledger, and the ledger cannot be a file without letting database and ledger diverge. The correct citation is supplied by the SRS itself, so no rule is invented and §35 is not engaged.

One secondary imprecision in the same docblock, worth folding into the same edit: migrator.js:23 says sequelize_meta "is excluded from the 64-table count by name". It is not excluded by name anywhere. assertSchemaMatchesSrs() (models/index.js:604) builds `actual` from `Object.values(models).map(m => m.getTableName())` — sequelize_meta has no Sequelize model, so it is never a candidate. The by-name exclusion that does exist is in a verify script (scripts/verify-jobs.js:536, filtering the mysqldump table list), not in the guard.

**Consequence:** No runtime effect; sequelize_meta is created and used correctly either way, and the 64-table guard is unaffected. The consequence is that the project's single documented carve-out from its hardest constraint — §29/§35, no 65th table — is defended, in the migrator's own header and in the checklist's ambiguity table, by a citation to two sections that say nothing about migrations, with two invented quotations attached. Anyone auditing the 65th physical table against §3 and §28 finds no support and has to rediscover §32:1565 independently.

**Fix / open question:** Two edits, both replacing a false citation with the true one the SRS supplies. (1) backend/src/database/migrator.js:19-23 — replace "the migration tooling that SRS §3 and §28 themselves mandate (\"Database Migrations\", \"Run migrations\")" with "the migration tooling that SRS §32's Working Method step 2 requires (\"Create database migration.\", SRS-extracted.md:1565)", and change "excluded from the 64-table count by name" to the accurate mechanism: it is not a registered Sequelize model, so `assertSchemaMatchesSrs()` never sees it. (2) docs/IMPLEMENTATION_CHECKLIST.md:133 — change the SRS-text cell from "§3/§28 mandate migrations" to "§32 Working Method step 2 requires database migrations", matching the citation the same table already makes correctly at :129. The exception stands; only its warrant changes.

**Evidence:** SRS: docs/SRS-extracted.md:1518 "Not Specified in Source Requirements — No tables beyond those listed above are introduced."; the ONLY migration reference in the document is docs/SRS-extracted.md:1565 "2. Create database migration." (§32); docs/SRS-extracted.md:57-77 is §3 in full and contains none; docs/SRS-extracted.md:1377-1396 is §28 in full and contains none. CODE: backend/src/database/migrator.js:19-22 "`sequelize_meta` is not application data — it is the ledger required by the migration tooling that SRS §3 and §28 themselves mandate (\"Database Migrations\", \"Run migrations\")"; backend/src/database/migrator.js:32 "const META_TABLE = 'sequelize_meta';" and :55-65 ensureMetaTable() creating it. CODE (the guard, which excludes it structurally not by name): backend/src/models/index.js:605 "const actual = Object.values(models).map((m) => m.getTableName());". CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:133 (the wrong citation) versus docs/IMPLEMENTATION_CHECKLIST.md:129 (the right one, four lines above).

---

### 40 — §32 Developer Working Method, SRS-extracted.md:1563 ("The exact daily development workflow specified in the source:") and step 8, SRS-extracted.md:1571 ("8. Create Git commit.")

Both halves verified. §32:1571 reads "8. Create Git commit." and the section is introduced at :1563 as "The exact daily development workflow specified in the source" — it is SRS text, not project convention. And `ls -a` at E:/School Managment System lists .claude, .gitignore, IMPLEMENTATION_PROGRESS.md, SRS_Multi-School-Management-System.docx, backend, deploy, docs, frontend; `test -d .git` fails. There is a .gitignore and no repository.

The defect the finding reports is the classification, and it is real: IMPLEMENTATION_CHECKLIST.md:913 introduces the block with "Also outstanding and not yet tracked as an SRS row, because neither is an SRS requirement:" and :915-916 puts the missing repository inside it, alongside the missing ESLint config. ESLint genuinely is not in the SRS (it appears nowhere in the document). Git is, at :1571. The stated reason for not tracking it is therefore false for one of the two items it covers.

The checklist's own practice contradicts it four hundred lines earlier: at :129 it treats §32 step 2 as binding enough to decide the ORM — "`sequelize-cli`-style migrations satisfy Working Method step 2 (\"Create database migration\")". A project that lets §32 step 2 pick its ORM cannot classify §32 step 8 as not-a-requirement.

The fix invents nothing: the SRS names the requirement and its line number, so reclassifying is a fact correction, and the substantive remedy (initialise the repository, commit) is literally what step 8 says. The finding's cross-checks on the neighbouring steps are also right — step 9's day-end report is IMPLEMENTATION_PROGRESS.md and step 10's carry-over discipline is the Known Issues register.

**Consequence:** Real and already visible in the project's own records. The checklist at :490 cannot attribute a module's provenance because there is no history, and the standing project rule that doc drift must be attributed from file mtimes exists only because of this absence. The classification error compounds it: an outstanding SRS requirement is filed where nobody auditing the SRS row list will look for it, so §32 step 8 is the one §32 step that is both testable read-only and failing, and it is untracked.

**Fix / open question:** Move the git item out of the docs/IMPLEMENTATION_CHECKLIST.md:913 block and give it its own row citing §32 step 8 (SRS-extracted.md:1571), status Not Started. Reword :913 so the "neither is an SRS requirement" reason covers only the ESLint config, which is genuinely absent from the SRS. The substantive remedy is `git init` plus commits, which is exactly what step 8 states — no rule is invented, and §35 is not engaged since no schema, catalogue or workflow is added.

**Evidence:** SRS: docs/SRS-extracted.md:1563 "The exact daily development workflow specified in the source:"; docs/SRS-extracted.md:1571 "8. Create Git commit." ABSENCE: `ls -a` at E:/School Managment System returns .claude, .gitignore, IMPLEMENTATION_PROGRESS.md, SRS_Multi-School-Management-System.docx, backend, deploy, docs, frontend — no .git; `test -d .git` fails. CHECKLIST (the mis-classification): docs/IMPLEMENTATION_CHECKLIST.md:913 "Also outstanding and not yet tracked as an SRS row, because neither is an SRS requirement:" with docs/IMPLEMENTATION_CHECKLIST.md:915-916 "…and the project is **not a git repository** — there is no history and no rollback." CHECKLIST (the project's own contrary treatment of §32): docs/IMPLEMENTATION_CHECKLIST.md:129. CONSEQUENCE ALREADY RECORDED: docs/IMPLEMENTATION_CHECKLIST.md:490 "whether that was session 15 continuing or a separate session is not knowable without git history".

---

### 41 — §33 Super Admin screen list, SRS-extracted.md:1579-1595 (16 screens); §14.1 School Settings, SRS-extracted.md:709

Every claim checks out, including the four contradictory statements and the mis-citation.

The SRS list is exactly sixteen, SRS-extracted.md:1580-1595: Dashboard, Organizations, Schools, Principals, Users, Plans, Modules, Features, Limits, Add-ons, Subscriptions, Invoices, Payments, Coupons, Reports, Settings.

The four statements, at the line numbers the finding gives (I confirmed each by grep, not by counting): :625 status cell "Completed" with "**All sixteen exist and are reachable — fifteen functional…**"; :627 "**15 of 16 built** (session 26)."; :652-653 "The row stays In Progress rather than Completed because / the screen §33 names does not exist"; :658 "**The four unbuilt:** Settings, and the three plan sub-screens."

All four allegedly-unbuilt pages exist on disk: frontend/src/app/(platform)/super-admin/settings/page.tsx (4,585 bytes, mtime 2026-09-05 07:25), plans/modules/page.tsx (4,951), plans/features/page.tsx (5,354), plans/limits/page.tsx (5,182). The full super-admin directory listing gives exactly the sixteen screens, and nav.ts:62-109 links every one — Modules/Features/Limits at :77-79 under the Catalogue heading, Settings at :109. So :625 is the current truth and :652-653 and :658 are stale.

The §16 mis-citation is real and appears twice, not once: :650 "`/school-settings` is school-scoped (§16)" and again at :658. SRS-extracted.md:709 is "## 14.1 School Settings"; :866 is "# 16. Attendance Management". The same row's current note at :625 already cites §14.1 correctly, so the file contradicts itself on this too.

The fix is deletion of superseded prose plus a section-number correction, both determined by the SRS and by the filesystem. Nothing is invented.

**Consequence:** The sole record for a sixteen-item SRS list says, in one block, that the row is Completed, that fifteen of sixteen are built, that it stays In Progress, and that four are unbuilt. A reader auditing §33 cannot tell which sentence is current and has to go to the filesystem to find out — which defeats the checklist's purpose. The doubled §16 mis-citation additionally points a reader at Attendance Management when looking for school settings. No runtime effect; this is the inverse of the usual failure mode (the row understates completed work), but the same defect class: the recorded status is not a reliable description of the code.

**Fix / open question:** In docs/IMPLEMENTATION_CHECKLIST.md, delete the superseded prose that the row itself supersedes: :627's "15 of 16 built" (or date-stamp it as the session-26 snapshot it was), :652-653's "The row stays In Progress rather than Completed because the screen §33 names does not exist", and :658's "**The four unbuilt:** Settings, and the three plan sub-screens" paragraph. Keep :625 as the single current statement. Correct "(§16)" to "(§14.1)" at :650 and at :658, matching the citation :625 already makes. The §33-names-Settings-but-§9-specifies-nothing gap is a separate, already-recorded row-4.3 issue and is untouched by this edit.

**Evidence:** SRS: docs/SRS-extracted.md:1579 "## Super Admin" with the sixteen items at :1580-1595; docs/SRS-extracted.md:709 "## 14.1 School Settings"; docs/SRS-extracted.md:866 "# 16. Attendance Management". CHECKLIST (the four incompatible claims): docs/IMPLEMENTATION_CHECKLIST.md:625, :627, :652-653, :658; the mis-citation at docs/IMPLEMENTATION_CHECKLIST.md:650 and again at :658. CODE (all four "unbuilt" pages exist): frontend/src/app/(platform)/super-admin/settings/page.tsx, frontend/src/app/(platform)/super-admin/plans/modules/page.tsx, frontend/src/app/(platform)/super-admin/plans/features/page.tsx, frontend/src/app/(platform)/super-admin/plans/limits/page.tsx. CODE (all sixteen linked): frontend/src/lib/nav.ts:62 (Dashboard) through :109 (Settings), with the three plan sub-screens at :77-79.

---

### 43 — §8 FR-TENANT-004, SRS-extracted.md:246-252

The checklist's own legend defines `Tested` as "not yet reachable in the running application — nothing mounts it". Both routers ARE mounted, inside buildApiRouter() and below the authenticate→resolveTenant→enforceTenant boundary. The row's own note says so and cites a passing suite (verify-school-setup.js exists; baseline.json records it at 168 assertions), which is exactly the definition of `Completed`. The status word contradicts both the legend and its own note.

**Consequence:** A reader auditing FR-TENANT-004 is told the school-settings and academic-session endpoints are unreachable when they are mounted and serving. Documentation only — no runtime effect.

**Fix / open question:** Change the status cell at IMPLEMENTATION_CHECKLIST.md:227 from `Tested` to `Completed`. No note change needed; the note already describes Completed.

**Evidence:** SRS docs/SRS-extracted.md:246-252 (FR-TENANT-004). CODE backend/src/app.js:499-500 `api.use('/school-settings', settingsRoutes); api.use('/sessions', sessionRoutes);`, below the boundary described at app.js:495-498. CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:227 status `Tested`, against the legend at :31. Suite present: backend/scripts/verify-school-setup.js; backend/tests/baseline.json records verify-school-setup.js at 168/168.

---

### 44 — §7 FR-AUTH-004/005, SRS-extracted.md:151-166 (code-comment claim)

Confirmed by exhaustive grep. Every write of `must_change_password: true` in backend/src is at account creation; there is no administrator-reset path at all. `PATCH /users/:id` explicitly strips both `password` and `must_change_password`, and the only reset flow (`POST /auth/reset-password`) is the account holder's own and sets the flag to false. The middleware docblock describes a second writer that does not exist.

**Consequence:** None at runtime. A maintainer reading the middleware that enforces the flag is told an admin-reset path exists and will not find one; the same repo states the opposite at users.validation.js:22-24, so the two comments contradict each other.

**Fix / open question:** Delete the clause "and an administrator who resets a user's password sets the same flag" from authenticate.js:204-205, leaving the bootstrap-seeder and creation paths as the stated writers.

**Evidence:** CODE (false claim) backend/src/middlewares/authenticate.js:204-206 "an administrator who resets a user's password sets the same flag". CODE (every actual writer of true) backend/src/database/seeders/04-super-admin.js:89, backend/src/modules/principals/principals.service.js:242, backend/src/modules/parents/parents.service.js:261 — all at creation. CODE (writers of false) backend/src/modules/auth/auth.service.js:590, :680. CODE (the strip) backend/src/modules/users/users.validation.js:22-24, :31-32.

---

### 45 — §7 FR-AUTH-005, SRS-extracted.md:161-166 (code-comment claim)

Verified on both sides. The service header's anti-enumeration table says forgot-password is "Always 200"; the controller returns 202 unconditionally, its own header says 202, and the checklist row says 202. The service docblock is the single stale statement, and it is in the file the header itself points readers at ("See the header" / "See the service" cross-reference each other).

**Consequence:** None at runtime — the contract (a constant status regardless of whether the address exists) holds either way. A reader opening the service to learn the guarantee reads the wrong number.

**Fix / open question:** Change "Always 200" to "Always 202" at auth.service.js:19.

**Evidence:** CODE (stale) backend/src/modules/auth/auth.service.js:19 "forgot-password   Always 200, whether or not the address is on file." CODE (actual) backend/src/modules/auth/auth.controller.js:222-226 `return ApiResponse.ok(res, null, { status: 202, ... })`, headed by auth.controller.js:30 "## `forgot-password` always answers 202". CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:209 "(always 202, no address oracle)".

---

### 49 — §9 FR-SADMIN-001…009, SRS-extracted.md:292-360; the suite the nine rows cite as evidence

Both counts are wrong, and I checked the route half rather than trusting the reviewer, because the comment says "routes under test" (which could legitimately be fewer than the §9 surface). It is not: the suite's own keyMap has exactly eighteen entries, one per §9 route, and the ROUTE_KEYS array beneath the "fourteen" comment has exactly eleven. So "fourteen" is wrong as a route count (18) and wrong as a key count (11), in all four places. The checklist itself states both correctly, so the defect is confined to the script's header.

**Consequence:** None at runtime — the suite tests all eighteen routes and denies all eleven keys regardless of what the comment says. A reader sent to this file by any of the nine FR-SADMIN rows as their evidence finds a header that contradicts the checklist twice over.

**Fix / open question:** In backend/scripts/verify-platform-modules.js replace "fourteen" with "eighteen" at :14, :106 and :578 (all three describe routes) and with "eleven" at :59 (which describes keys in `denied_permissions`).

**Evidence:** CODE backend/scripts/verify-platform-modules.js:14, :59, :106, :578 all say "fourteen"; ROUTE_KEYS at :107-119 holds eleven entries; the keyMap at :1550-1568 holds eighteen entries and the loop at :1571 exercises every one. Route surface independently: platform.routes.js 1, organizations.routes.js 4, schools.routes.js 10, principals.routes.js 3 = 18. CHECKLIST (correct) docs/IMPLEMENTATION_CHECKLIST.md:230 "eighteen endpoints", :239-241 "all eleven §9 keys".

---

### 50 — FR-SADMIN-001, SRS-extracted.md:297-298

The comment's claim is false as written and I confirmed the divergence line by line. getDashboard builds three scopes from three different helpers with two different precedence orders: organizationsService.scopeFor and schoolsService.scopeFor test `tenant.organizationId` first, while tenantWhere tests `tenant.schoolId` first and returns immediately. resolveTenant deliberately honours a `school_id` on an organization-scoped user's row. So for an organization_admin carrying users.school_id, four figures come back organization-wide and seven come back for one school, in one response — not "every figure narrowed to their own organization by the same helpers". Reachability is genuinely limited (no POST /users; PATCH /users/:id cannot set school_id; no seeder creates such a row), so the mixed-scope response needs a hand-written row — but the comment is false unconditionally, and correcting it invents nothing.

**Consequence:** Documentation-level today: FR-SADMIN-001's actor is the Super Admin and the figures are correct for that caller, and the organization_admin shape that triggers the mix has no creation path in the API. The comment states a property the code does not have.

**Fix / open question:** Correct platform.service.js:56-58 to say what the code does — an organization-scoped caller gets organization-wide organization/school counts, and tenantWhere gives an explicit `school_id` precedence, so an organization_admin whose users.school_id is set gets the row-level figures narrowed to that school. Do NOT change the precedence itself without an answer to a question the SRS does not supply: FR-SADMIN-001 names only the Super Admin, and §5:97 declines to specify any organization-admin workflow, so nothing in the source says which scope a narrowed organization_admin's dashboard should use.

**Evidence:** CODE (false claim) backend/src/modules/platform/platform.service.js:56-58. CODE (three scopes) platform.service.js:123-125 feeding the aggregates at :150-165. CODE (precedence A) backend/src/modules/organizations/organizations.service.js:49-52 and backend/src/modules/schools/schools.service.js:84-94 — organizationId tested before schoolId. CODE (precedence B) backend/src/models/index.js:659-668 — "An explicit school scope always wins", schoolId returns first. CODE backend/src/middlewares/resolveTenant.js:143-147. SRS docs/SRS-extracted.md:297-298.

---

### 51 — §12 FR-SUB-010, FR-SUB-015 — SRS-extracted.md:571-578, :609-616

Confirmed by directory listing. `backend/src/jobs/` exists and contains cron.js, worker.js, handlers/ and tasks/ with five task files; subscriptionLifecycle.js registers the sweep hourly as the first task in cron.js's ORDER. Both rows state the opposite, and the same file's row 5.9 records the cron layer as Completed, so the checklist contradicts itself on the same page.

**Consequence:** A reader auditing FR-SUB-010 or FR-SUB-015 is told the date-driven half never fires — that trials never end and automatic renewals never run — when an hourly task calls the sweep. Documentation only; the sweep itself works.

**Fix / open question:** In IMPLEMENTATION_CHECKLIST.md:430 replace "the cron trigger is Phase 5.9 and `src/jobs/` does not exist" with the trigger that exists (`src/jobs/tasks/subscriptionLifecycle.js`, hourly at '5 * * * *', first in cron.js's ORDER); delete the "Only the scheduler ... is outstanding" sentence at :435. Row 809's identical staleness about `src/jobs/handlers/` and `src/jobs/worker.js` (both present) is outside this slice but is the same edit.

**Evidence:** CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:430 "the cron trigger is Phase 5.9 and `src/jobs/` does not exist"; :435 "**Only the scheduler that calls the sweep is outstanding** (Phase 5.9)"; contradicted by :810 "| 5.9 | Cron jobs ... | Completed |". CODE backend/src/jobs/ contains cron.js, worker.js, handlers/index.js, tasks/{couponExpiry,databaseBackup,invoiceOverdue,notificationDispatch,subscriptionLifecycle}.js; backend/src/jobs/tasks/subscriptionLifecycle.js:17-20 `name: 'subscription-lifecycle', schedule: '5 * * * *', run: (options) => subscriptionsService.runLifecycleSweep(options)`; backend/src/jobs/cron.js:72-73 registers it first.

---

### 54 — §12.5 / FR-SUB-015 — SRS-extracted.md:566-569, :609-616

Confirmed. FR-SUB-015 sits inside "# 12. Subscription Lifecycle" (heading at :529), under §12.5 Renewal (:566-569); "# 13. Invoice, Payment & Coupon System" begins at :618 and defines no renewal, trial, grace or expiry behaviour. The task file cites "SRS §13 / FR-SUB-015" twice and cron.js's table gives §13 as the section the sweep was waiting on, while its sibling rows correctly give §13.1 for invoice-overdue and §13.2 for coupon-expiry — so the column is unambiguously SRS sections and this row is the wrong one.

**Consequence:** None at runtime — the task genuinely runs the §12 sweep. A reader following the citation lands in the invoice chapter and finds no renewal requirement there.

**Fix / open question:** Replace §13 with §12.5 (or §12) in all three places: subscriptionLifecycle.js:6, :19 and cron.js:17.

**Evidence:** SRS docs/SRS-extracted.md:529 "# 12. Subscription Lifecycle"; :566 "## 12.5 Renewal"; :609 "FR-SUB-015 — Subscription Renewal"; :618 "# 13. Invoice, Payment & Coupon System". CODE backend/src/jobs/tasks/subscriptionLifecycle.js:6 "SRS §13 / FR-SUB-015"; :19 "description: 'Trial ends, renewals, past-due, grace, expiry (SRS §13, FR-SUB-015)'"; backend/src/jobs/cron.js:17 "| `subscription-lifecycle` | §13 | ...".

---

### 55 — §14.1 School Settings, SRS-extracted.md:709 vs §16 Attendance, SRS-extracted.md:866

Confirmed on both sides. School Settings is §14.1 (SRS:709); §16 is Attendance Management (SRS:866). The Phase 4 prose annotates `/school-settings` with "(§16)" twice. The same checklist file cites §14.1 correctly at :497 and :625, and every code site does too, so this is two isolated typos, not a house convention.

**Consequence:** None at runtime. A reader tracing the platform-Settings gap through this paragraph is sent to the attendance section.

**Fix / open question:** Change "(§16)" to "(§14.1)" at IMPLEMENTATION_CHECKLIST.md:650 and :658.

**Evidence:** SRS docs/SRS-extracted.md:709 "## 14.1 School Settings"; :866 "# 16. Attendance Management". CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:650 and :658 both "`/school-settings` is school-scoped (§16)", against the same file's "§14.1" at :497 and :625. CODE backend/src/modules/settings/settings.routes.js:8-9 and backend/src/app.js:489 both cite §14.1.

---

### 56 — §14.1 / FR-SCHOOL-001, SRS-extracted.md:738-744

Confirmed by reading the function. show() does `findOne` and returns `row || virtualDefaults(school)`; virtualDefaults returns `{ id: null, ... }` and never inserts. The file header says the opposite, and the same file states the truth 30 lines later with the reason. The checklist row is correct, so the defect is entirely in the module header — the first thing a reader opens.

**Consequence:** None at runtime. A reader trusting the header expects a `school_settings` row to exist after any GET — the exact invariant the verify suite asserts is false — and the file contradicts itself internally.

**Fix / open question:** Rewrite settings.service.js:6-7 to match :36-37: GET reads the row and falls back to virtual defaults without inserting; PATCH is the upsert.

**Evidence:** CODE (false) backend/src/modules/settings/settings.service.js:6-7 "GET find-or-creates with the column defaults". CODE (true) settings.service.js:36-37 "GET does not insert: a settings screen opening is not a write, and inserting here would create a row with no audit trail and no `school.settings.manage` check. PATCH is the upsert."; :69-73 `async function show(req) { ... const row = await db.SchoolSetting.findOne(...); return row || virtualDefaults(school); }`; :40-42 virtualDefaults returns `id: null`. CHECKLIST (correct) docs/IMPLEMENTATION_CHECKLIST.md:497 "`GET` find-or-**virtuals** and deliberately does not insert (asserted: the table is still empty after a GET)".

---

### 57 — §17 SRS-extracted.md:903; §18 SRS-extracted.md:934

Verified: a grep for "18.1" or "18.2" over the whole SRS returns nothing. §17 is Fee Management (:903) and §18 is Finance Management (:934), whose only child heading is "## Functional Requirements" — §18 has no numbered subsections. So the Fees screen cites both a nonexistent subsection and the wrong chapter (Fees is §17, not §18), and the Finance screen cites a nonexistent subsection of the right chapter.

**Consequence:** None at runtime. A reader checking the Fees screen against its requirement is sent to Finance Management; a reader following either subsection number finds no such heading.

**Fix / open question:** Change fees/page.tsx:4 to "SRS §17" and finance/page.tsx:4 to "SRS §18".

**Evidence:** SRS docs/SRS-extracted.md:903 "# 17. Fee Management", :934 "# 18. Finance Management", next heading :942 "## Functional Requirements"; no "18.1"/"18.2" anywhere in the file. CODE frontend/src/app/(school)/school/fees/page.tsx:4 "Fees — SRS §18.1, ..."; frontend/src/app/(school)/school/finance/page.tsx:4 "Finance — SRS §18.2, ...".

---

### 58 — §19 (:968), §20.2 (:1049), §21 (:1138) — checklist rows :556, :560, :568, :578 and the inventory table :65-73

Confirmed against the manifest, and the drift is now wider than the finding reports. Every cited figure disagrees with backend/tests/baseline.json, which npm test gates on: exams 206 and 217 vs 219, homework 98 vs 109, ai 169 vs 188, reports 98 vs 106; and the inventory also carries attendance 79 vs 80 and fees 163 vs 170, with a "thirty suites / 4,613" total against a recorded 38 suites / 5,142. (The findings document's own intro figure of 5,133 is itself now behind — baseline reads 5,142.) The status legend defines `Completed` as a check that "passes today", so a row citing a count the manifest no longer holds is not evidence of today. I could not run the suites (read-only), so I am asserting only that the two artefacts disagree — not which is right.

**Consequence:** Nine or more rows and the head-of-file inventory present figures the recorded manifest contradicts, so the evidence a reader is pointed at cannot be reconciled with the suite that actually runs. No runtime effect.

**Fix / open question:** Re-measure and rewrite the inventory table (:60-73) and the four row figures (:556, :560, :568, :578) from backend/tests/baseline.json — the values already exist there, so nothing is invented; also update the "thirty suites / 4,613" line at :36-37 to the recorded 38 / 5,142 and re-date the table. Row 5.3's AI figure of 188 already agrees with the manifest and needs no change.

**Evidence:** CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:65 "| `verify-exams.js` | 206 |", :67 "| `verify-homework.js` | 98 |", :71 "| `verify-ai.js` | 169 |", :73 "| **Total** | **4,613** |"; :556 "206 / 206", :560 "217 / 217", :568 "98 / 98", :578 "169 / 169"; :36-37 "one of the thirty ... suites ... total 4,613 assertions". MANIFEST backend/tests/baseline.json:8-9 "suiteCount": 38, "totalAssertions": 5142; verify-exams.js 219, verify-homework.js 109, verify-ai.js 188, verify-reports.js 106, verify-attendance.js 80, verify-fees.js 170.

---

### 59 — FR-EXAM-001 SRS-extracted.md:1001; FR-EXAM-004 SRS-extracted.md:1026

Confirmed on all three sides. Both FRs name Teacher in their Actor line. POST /exams requires `exams.manage` and POST /exams/:id/results requires `results.generate`, and the seeded ROLES.TEACHER list holds neither (it has exams.view, marks.enter, results.view). The narrowing is defensible and cannot change — the 109 permissions and 353 grants are fixed by §29/§30/§35 — but the two checklist rows read as unqualified `Completed` with no mention of the withheld actor, while the project's own precedent for the identical situation, FR-ATT-003 at :535, states it in the row itself. The deviation is recorded only in the progress log.

**Consequence:** No runtime consequence — the refusal is deliberate and correct for the fixed catalogue. Two rows a reader consults for status omit a deviation the project's own convention says belongs there, so the same deviation is disclosed in one place and hidden in two.

**Fix / open question:** Add to the FR-EXAM-001 and FR-EXAM-004 rows the note FR-ATT-003 already carries at :535, naming Teacher as the actor the seeded catalogue does not reach and citing the fixed 109/353 catalogue as the reason. Do not change the grants.

**Evidence:** SRS docs/SRS-extracted.md:1001 "Actor / Role: Principal / School Admin / Teacher" (FR-EXAM-001); :1026 the same for FR-EXAM-004. CODE backend/src/modules/exams/exams.routes.js:181-183 `router.post('/', requirePermission('exams.manage'), ...)`; :232-234 `router.post('/:id/results', requirePermission('results.generate'), ...)`; backend/src/config/permissions.js:292-324 — the teacher list has 'exams.view', 'marks.enter', 'results.view' and neither withheld key. CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:556 and :559 (silent) against :535 ("**Narrower than the FR's actor list, deliberately:**"). Recorded only at IMPLEMENTATION_PROGRESS.md:2602-2608.

---

### 61 — §22 FR-REPORT-001, SRS-extracted.md:1188-1194

Both independent measures agree against the checklist: baseline.json records verify-reports.js at 106 assertions / 106 pass lines, and a literal count of assertion calls in the script is also 106. The row and the inventory both say 98. Row 5.4 explains the delta itself ("plus 8 new checks in verify-reports.js"), so 98 + 8 = 106 reconciles exactly — the two places that carry the figure were simply not updated. The inventory table at :72 is at least dated; :585 carries no qualifier and reads as current.

**Consequence:** The FR-REPORT-001 row understates its own evidence by eight assertions. No runtime effect; the same staleness family as finding 58.

**Fix / open question:** Change 98 to 106 at IMPLEMENTATION_CHECKLIST.md:585 and :72. The separate claim that the inventory has no verify-notifications.js row is also true and is fixed by adding one; both values come from baseline.json, so nothing is invented.

**Evidence:** CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:585 "`verify-reports.js` → **98 / 98, exit 0**"; :72 "| `verify-reports.js` | 98 |"; :805 "`verify-pdf.js` → **20 / 20** plus 8 new checks in `verify-reports.js`". MANIFEST backend/tests/baseline.json:120-123 "verify-reports.js": { "assertions": 106, "passLines": 106 }. COUNT `grep -c "^\s*\(check\|assert\|ok\)(" backend/scripts/verify-reports.js` → 106.

---

### 62 — §22 — docs/IMPLEMENTATION_CHECKLIST.md:583-586

Verified by counting delimiters rather than by eye. Section 3.Q's header and separator each carry four pipes (three columns) while both data rows carry five (four cells); section 3.R immediately below carries five pipes on every line including its header. GitHub-flavoured Markdown truncates each row to the header's column count, so the fourth cell — the entire Notes text for both FR-REPORT rows — is dropped from the rendered table.

**Consequence:** Real and reader-visible: in any rendered view of the checklist, the delegation reasoning, the two-permission rule and the export evidence for FR-REPORT-001 and FR-REPORT-002 are invisible. Only someone reading the raw file sees them.

**Fix / open question:** Append "| Notes |" to the header at :583 and a fourth "---" cell to the separator at :584, matching 3.R at :590-591.

**Evidence:** docs/IMPLEMENTATION_CHECKLIST.md:583 "| ID | Requirement | Status |" (4 pipes) and :584 "|---|---|---|" (4 pipes) against :585 and :586 (5 pipes each); contrast :590-591 "| ID | Requirement | Status | Notes |" / "|---|---|---|---|" (5 pipes, matching its rows).

---

### 66 — §27 / FR-DEPLOY-001, SRS-extracted.md:1358 and :1374

The status contradicts its own note and the legend. FR-DEPLOY-001's Functional Behavior includes "Database Backup, Logging, and Monitoring are configured for production", and §27 lists Monitoring among the deployment items. What exists is deploy/monitoring/README.md — 863 lines of operator prose and copy-paste shell whose own section 0 is headed "What has been verified, and what has not" — plus two health endpoints. Nothing in the repo watches those endpoints and nothing is scheduled to, which the row's own note says. The legend defines `Completed` as code that exists with a passing check covering it; health endpoints are the thing monitored, not the monitoring, and the legend supplies the accurate word: `In Progress` means "Part of the requirement is done. The Notes column says which part, and which part is not" — which is exactly what that note already does. The fix comes from the checklist's own legend, not from an invented rule.

**Consequence:** A reader scanning the status column sees a requirement marked done whose own note says the essential part is absent. Real but confined to the tracking document; the honest note limits the damage.

**Fix / open question:** Change row 7.11's status at IMPLEMENTATION_CHECKLIST.md:909 from `Completed` to `In Progress`, whose legend definition ("the Notes column says which part is done and which is not") the existing note already satisfies. If the runbook is judged the honest ceiling for a repo artifact, say that in the note rather than in the status word.

**Evidence:** SRS docs/SRS-extracted.md:1358 "- Monitoring"; :1374 "- Database Backup, Logging, and Monitoring are configured for production." CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:909 status `Completed` with the note ending "No monitoring is attached to them"; legend at :29-33. ARTIFACT deploy/monitoring/README.md:1 "# Monitoring — SRS §27" and :24 "## 0. What has been verified, and what has not" — a runbook, with no installed unit, cron entry or process; backend/src/jobs/tasks/ contains no monitoring task.

---

### 68 — §27 / FR-DEPLOY-001, SRS-extracted.md:1346-1375

Measured directly: the six deploy artifacts total 5,332 lines (991 + 725 + 863 + 1049 + 1100 + 604), not 5,312. The companion figure in the same sentence is sound — verify-deploy.js contains exactly 52 top-level check() calls — so only the line count is wrong. On cause I can say nothing beyond what the filesystem shows and will not guess: the checklist was written after the last deploy edit, so write ordering does not explain it.

**Consequence:** None beyond the figure itself. A twenty-line discrepancy in one evidence citation.

**Fix / open question:** Change 5,312 to 5,332 at IMPLEMENTATION_CHECKLIST.md:884. Leave the 52-assertion figure alone; it is correct.

**Evidence:** CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:884 "`deploy/` — six artifacts, 5,312 lines, verified by `scripts/verify-deploy.js` (52 assertions ...)". MEASURED `wc -l` over deploy/: env/production.env.example 991, logrotate/msms 725, monitoring/README.md 863, mysql/production.cnf 1049, nginx/msms.conf 1100, pm2/ecosystem.config.js 604 = 5,332. `grep -c '^\s*check(' backend/scripts/verify-deploy.js` → 52 (accurate).

---

### 69 — §33 SaaS Engine, SRS-extracted.md:1628 ("- Overage")

Confirmed on both halves. The deferral at :478 sends the reader to 3.G for Overage, and 3.G's §33 row at :436 is headed "Feature overrides, custom limits, custom pricing" and neither mentions overage nor implements it — so the cross-reference dangles. And overage is not outstanding at all: invoices.service.js has a dedicated overageLinesFor() that reads usage_records with overage_amount > 0 across the billed period and emits `item_type: 'overage'` lines, and usageService writes overage_value and overage_amount on the usage row. Both live inside 3.H's own module.

**Consequence:** A reader auditing §33's Overage line is deferred to a row that does not cover it and never learns the feature is built and invoicing. Documentation only.

**Fix / open question:** At IMPLEMENTATION_CHECKLIST.md:478 drop "Overage" from the list of items that "remain 3.G" and point it at the implementation instead (invoices.service.js overageLinesFor + usageService's overage_value/overage_amount), leaving custom limits and feature overrides deferred as they are.

**Evidence:** SRS docs/SRS-extracted.md:1628 "- Overage". CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:478 "Overage / custom limits / feature overrides remain 3.G"; the target row :436 "| §33 | Feature overrides, custom limits, custom pricing | Completed |" — no overage. CODE backend/src/modules/invoices/invoices.service.js:389 "§33's *Overage*, read off `usage_records` for a billing period.", :401 `async function overageLinesFor(schoolId, subscriptionId, periodStart, periodEnd, transaction)`, :421 `item_type: 'overage',`; backend/src/services/usageService.js:519-523 `const overage = overageFor(limit, used); ... row.set('overage_value', overage.value); row.set('overage_amount', overage.amount);`.

---

## real-blocked — needs an SRS decision, not code (11)

### 10 — FR-SADMIN-007 (SRS-extracted.md:338-345, preconditions :341, behaviour :343-344); §9.2 "Assign Principal" / "Change Principal" (:277-278)

The added precondition is real. FR-SADMIN-007 states exactly two preconditions — "School record and Principal account exist" (:341) — and `assignPrincipal` enforces a third: `if (Number(user.school_id) !== Number(school.id))` → 422 (schools.service.js:435-439). So a Principal account that exists but belongs to another school cannot be selected, which the SRS's literal text permits. I also confirmed the escape hatch the docblock names does not exist: users.validation.js:25-27 lists `organization_id` / `school_id` among the fields "deliberately not accepted", the `update` schema (users.validation.js:108-115) carries only name, email, username, phone, status, locale, users.routes.js declares no POST (only 3 GETs, 1 PATCH, 1 PUT), and the sole writer of `users.school_id` in the whole of src/ is `principals.service.js:231` at account creation. So the 422's own remediation hint — "Change the user's school before assigning them here" — instructs the caller to perform an operation no endpoint offers. What is NOT blocked is the ordinary workflow: FR-SADMIN-009 creates a Principal already "associated with the selected school" (:353-358), so assign and re-assign within one school both work, and §9.2's "Change Principal" is satisfied. Only a cross-school move is refused. Removing the guard is where this blocks: `users.school_id` is what scopes every request that user makes and every row that references them, and the SRS never says whether "links the Principal to the school" moves the user's tenancy or only writes `schools.principal_id`. Leaving it alone produces a Principal on record at school B while operating in school A's scope; moving it is a tenant transfer with cascading effects that no requirement describes. Either choice is a rule I would be inventing, which §35 forbids.

**Consequence:** A Super Admin cannot move an existing Principal from school A to school B: the request is refused 422, and the refusal's own hint points at an operation the API does not expose, so the caller has no next step. The only route to §9.2's "Change Principal" across schools is creating a second Principal account for the same person. Within a school, assign and change work normally.

**Fix / open question:** The SRS does not answer: when the selected Principal's account belongs to a different school, does FR-SADMIN-007's "System links the Principal to the school" (:344) also move `users.school_id` — and with it that user's tenant scope under §8 and every record already referencing them — or does it write only `schools.principal_id`, leaving the Principal on record at one school while scoped to another? Nothing in §9.2, FR-SADMIN-007 or FR-SADMIN-009 says, and §33's Users surface is not given a transfer operation. Two corrections ARE available without that answer and should be made regardless: reword the hint at schools.service.js:437 so it stops naming a non-existent operation, and add the same-school precondition to checklist row 252, which currently understates what the endpoint refuses.

**Evidence:** SRS: docs/SRS-extracted.md:341 "Preconditions: School record and Principal account exist."; :343-344 "- Super Admin selects a Principal for the school. / - System links the Principal to the school, replacing any prior assignment when changing."; :357 (FR-SADMIN-009) "- System creates the Principal account with the submitted status." / :358 "…associated with the selected school." CODE: backend/src/modules/schools/schools.service.js:435-439 "if (Number(user.school_id) !== Number(school.id)) { throw ApiError.validation('The selected Principal does not belong to this school', { user_id: \"Change the user's school before assigning them here\" }); }"; docblock at backend/src/modules/schools/schools.service.js:403-406 ("Moving a Principal between schools … belongs to the user-management surface (SRS §33 \"Users\"), not to this endpoint"). THE SURFACE REFUSES IT: backend/src/modules/users/users.validation.js:25-27; users.validation.js:108-115 (six-field update schema); backend/src/modules/users/users.routes.js:66,78,84,91,101 (no POST, no school_id writer). SOLE WRITER: backend/src/modules/principals/principals.service.js:231 "school_id: school.id,". CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:252 (records the FK cycle and `previousPrincipal`, never the same-school precondition).

---

### 11 — §10.2 Plan fields (SRS-extracted.md:381-388, Status at :385), FR-SUB-001 (:412-413), FR-SUB-004 (:434, :436)

Both halves of the claim check out. §10.2:385 lists Status among the seven plan fields and FR-SUB-001:412 repeats them verbatim as what the Super Admin submits, followed by :413 "System stores the plan in subscription_plans." The create schema at plans.validation.js:144-154 carries ten keys — name, code, description, visibility, is_recommended, display_order, trial_days, grace_period_days, default_renewal_mode, tier_rank — and no `status`; `fields.status` exists at :125 but is used only by filters. Because validate() runs with `stripUnknown: true` on the body (middlewares/validate.js:51), a submitted Status is silently discarded, and plans.service.js:349 then hard-codes `status: PLAN_STATUS.INACTIVE`. So the caller gets 201 with a status they did not submit and no signal that the field was dropped. The FR-SUB-004 half is real too: its only precondition is "Plan record exists" (:434) and its behaviour is a toggle (:436), but setStatus refuses with 409 PLAN_NOT_PRICEABLE unless an active plan_prices row exists (plans.service.js:456-464). This blocks rather than resolves because the SRS never enumerates Status's legal values (PLAN_STATUS in code has three: active, inactive, archived — constants.js:77-81) and never says whether a plan may be born Active. Honouring :412 literally forces three inventions: which of the three values POST /plans accepts; whether Status=Active at create bypasses the active-price requirement the code imposes on FR-SUB-004 (a requirement the SRS itself nowhere states, but whose removal would let a subscription be sold against a plan with no plan_prices row to denormalise from); and whether a birth-archived plan stamps `archived_at`, since the module keeps status and timestamp in step through one transition table precisely so they cannot disagree. The finding is also right that the checklist is honest here — :364 and :367 both record the deviations — though :364 opens with "All seven source fields accepted", which the same sentence then contradicts.

**Consequence:** A Super Admin who submits the seven fields FR-SUB-001 names gets 201 and a plan whose Status is not the one sent, with no error and no warning — the field vanishes at the validator. Reaching Active then requires a second call that carries an undocumented precondition, so a plan cannot be created active in one step at all. Checklist:364's opening claim "All seven source fields accepted" is false on its face and is contradicted by its own next sentence.

**Fix / open question:** The SRS does not answer two questions. (1) §10.2:385 and :412 name Status as a submitted create field but never enumerate its legal values — may POST /plans accept `archived`, and if so must it stamp `archived_at` to keep the column and the timestamp in step? (2) May a plan be born `active` while holding no `plan_prices` row, given that FR-SUB-006 makes pricing a separate later operation and FR-SUB-004 defines Active as "available for new subscriptions" — i.e. does a create with Status=Active bypass the active-price precondition the code imposes on FR-SUB-004, a precondition the SRS itself never states? Until both are answered, accepting `status` on create means inventing a rule. One correction needing no answer: delete "All seven source fields accepted" from docs/IMPLEMENTATION_CHECKLIST.md:364 — six of the seven are accepted, as the rest of that same cell says.

**Evidence:** SRS: docs/SRS-extracted.md:381-388 "Plan fields: - Name / - Code / - Description / - Status / - Public/Private / - Recommended / - Display Order" (Status at :385); :412 "- Super Admin submits plan fields: Name, Code, Description, Status, Public/Private, Recommended, Display Order."; :413 "- System stores the plan in subscription_plans."; :434 "Preconditions: Plan record exists."; :436 "- Super Admin toggles plan status between Active and Deactivated." CODE: backend/src/modules/plans/plans.validation.js:144-154 (`const create` — ten fields, no `status`); plans.validation.js:125 (`fields.status`, filters only); plans.validation.js:13-25 ("**`status`, anywhere.** … It is refused on **create** as well"); backend/src/middlewares/validate.js:51 "body: { ...BASE_OPTIONS, stripUnknown: true },"; backend/src/modules/plans/plans.service.js:347-351 "plan = await db.SubscriptionPlan.create({ ...payload, status: PLAN_STATUS.INACTIVE, archived_at: null });"; plans.service.js:456-464 "if (!priceable) { throw ApiError.conflict('This plan has no active price…', { code: 'PLAN_NOT_PRICEABLE' … }) }"; backend/src/config/constants.js:77-81 (PLAN_STATUS: active / inactive / archived). CHECKLIST (records it, but see below): docs/IMPLEMENTATION_CHECKLIST.md:364 "All seven source fields accepted … **A new plan is born `inactive` whatever the body says** … `status` is *stripped* rather than refused"; :367 "**Activation requires an active price** — 409 `PLAN_NOT_PRICEABLE`".

---

### 18 — FR-SCHOOL-001 / FR-SCHOOL-002 (SRS-extracted.md:738-753), §14.1/§14.2 (:709-723) versus §33 'School' MVP list (:1583,1597-1614) and §35 (:1665-1671)

Every factual claim checks out. FR-SCHOOL-001 and FR-SCHOOL-002 both name 'Principal / School Admin' as actor and describe a USER configuring settings and creating/activating/closing sessions. §33's School list contains exactly seventeen entries — Principal Dashboard, Teachers, Staff, Students, Parents, Classes, Sections, Subjects, Attendance, Fees, Finance, Exams, Results, Timetable, Homework, Library, Documents — and neither a Settings nor a Sessions screen. The implementation follows §33 exactly: SCHOOL_NAV (frontend/src/lib/nav.ts:120-167) has those seventeen and nothing else; ls of frontend/src/app/(school)/school/ returns attendance, classes, documents, exams, fees, finance, homework, library, page.tsx, layout.tsx, parents, results, staff, students, subjects, teachers, timetable — no settings/, no sessions/. A whole-frontend grep for 'school-settings' returns only three prose comments (super-admin/settings/page.tsx:14,:63 and nav.ts:103), and a grep for '/sessions' as a path literal returns nothing at all. The backend surface is live and complete (app.js:499-500; nine routes: GET/PATCH /school-settings and the seven session routes), guarded by permissions that already exist in the fixed catalogue (school.settings.view/manage, sessions.view/manage), so the missing piece is only the screen. It is blocked because closing it means choosing between two SRS statements that point opposite ways and neither of which yields: §33 is 'The final MVP requirements checklist, strictly per the source' — an enumeration, not a minimum — while §32 step 6 makes 'Connect frontend UI' part of every feature's method, and §35 lists 'Additional workflows' among the things this SRS explicitly does not decide. The project has already ruled the same way once, at checklist row 4.3, where the platform-side Settings gap was recorded rather than resolved. Building an eighteenth and nineteenth School screen would be adding a workflow §33 does not name; leaving it means two FRs with a human actor have no human surface. The documentation half is unblocked and worth doing regardless: row 4.4 (IMPLEMENTATION_CHECKLIST.md:667) records '17 MVP screens | Completed' with two limitations noted (students list columns, STUDENT_STATUS values) and no mention of either absence, while row 4.3 (:625) spends a paragraph on the analogous platform gap and rows 3.I (:497) and FR-TENANT-004 (:227) record the backend as Tested without noting that no screen calls it.

**Consequence:** A Principal or School Admin cannot configure any of §14.1's ten settings, and cannot create, activate or close an academic session, without an API client — nine working endpoints have no caller in the product. It also propagates: frontend/src/app/(school)/school/classes/page.tsx:20-28 records that the class list cannot offer an academic-session filter because filling the picker needs GET /sessions, which no screen calls. FR-SCHOOL-002's outcome, 'School operates within a defined, correctly-stated academic session', rests on an operation no user-facing surface performs.

**Fix / open question:** Blocked on this question, which the SRS does not answer: is §33's 'School' enumeration a CEILING on the MVP frontend — making FR-SCHOOL-001/002 deliberately API-only for the MVP, the same reading row 4.3 took for platform Settings — or does §32's per-feature 'Connect frontend UI' step oblige a screen for every FR whose actor is a human, so that §33's list is a floor and two more School screens are required? §35 lists 'Additional workflows' among the gaps it declines to fill, so answering it either way is a decision, not a derivation. Unblocked and worth doing now regardless of that answer: record the absence in checklist row 4.4 (IMPLEMENTATION_CHECKLIST.md:667) the way row 4.3 records the platform-side one — nine endpoints built and tested, no screen calls them, §33 names neither screen.

**Evidence:** SRS: docs/SRS-extracted.md:738-744 'FR-SCHOOL-001 ... Actor / Role: Principal / School Admin ... - User configures Logo, Name, Address, Phone, Email, Website, Favicon, Theme, Currency, and Timezone.'; :745-753 'FR-SCHOOL-002 ... - User creates a new academic session. - User activates an academic session. - User closes an academic session.'; :1577 'The final MVP requirements checklist, strictly per the source:'; :1597-1614 the seventeen School screens; :1666-1671 §35 'Additional workflows' among gaps this SRS does not fill; :1563-1571 §32 step 6 'Connect frontend UI.' CODE: frontend/src/lib/nav.ts:120-167 SCHOOL_NAV (no settings, no sessions); frontend/src/app/(school)/school/ has no settings/ or sessions/ directory; grep 'school-settings' over frontend/src → frontend/src/app/(platform)/super-admin/settings/page.tsx:14,63 and frontend/src/lib/nav.ts:103, all comments; grep for a '/sessions' path literal over frontend/src → no hits; backend/src/app.js:499-500 api.use('/school-settings', settingsRoutes) / api.use('/sessions', sessionRoutes); backend/src/modules/settings/settings.routes.js:25-32 (GET/PATCH, school.settings.view/manage); backend/src/modules/sessions/sessions.routes.js:6-13 (seven routes, sessions.view/manage). CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:667 row 4.4 'Completed' plus :698-703 the only two limitations recorded; :625 row 4.3 documenting the platform Settings gap; :497 and :227 recording the backend as Tested.

---

### 20 — FR-FEE-001 Expected Outcome (SRS-extracted.md:923) and §17 component list (:907); §35 'Additional business rules' (:1665-1671)

The mechanism is real and I confirmed it end to end. alreadyAssigned() at backend/src/modules/fees/fees.service.js:266-278 builds its where as { school_id, student_id IN (...), component, period_month: periodMonth || null, status != waived }, so when no period is supplied Sequelize emits `period_month IS NULL` and the guard matches every prior period-less fee of that component for that student. assign() calls it at :361-368 with rows[0].period_month and raises 409 FEE_PERIOD_ALREADY_ASSIGNED. period_month is genuinely optional — fees.validation.js:137 'period_month: fields.period_month' with no .required() — and is normalised to null at :335. The consequence follows: a student who carries one non-waived exam_fee assigned without a period can never be assigned a second exam_fee without a period, for the life of the record, and the refusal reads 'Those students already carry this fee for this period' when there is no period. §17 lists Exam Fee, Admission Fee and Transport Fee as components (:906-909) and states no once-per-student restriction; FR-FEE-001's outcome is only that the structure is 'available for assignment to students'. The suite does not cover it: the duplicate test at verify-fees.js:935-959 sets period_month '2025-05-01' on both calls and the follow-on at :961-975 uses '2025-06-01'; the one period-less assignment in the file (:871-886, the transport/fine-drift case) is never repeated, so neither the refusal nor its absence is asserted for a null period. It is blocked rather than fixable because the guard itself is an interpretation the SRS did not ask for — §29 gives student_fees no unique index, §35 forbids adding one, and the module chose (student, component, period_month) as the identity of a fee because that is the shape of the schema's lookup index. Every available repair turns on a rule §17 does not state. Relaxing the guard for a null period reopens the double-click double-bill the guard exists to prevent, and for the exact components most likely to be assigned without a period. Making period_month required adds a mandatory field §17 never names. Falling back to fee_structure_id when the period is null contradicts the reason the triple was chosen (:255-258: two different structures charging monthly_fee for May are the same double-bill). Each is a new business rule.

**Consequence:** Observable and blocking. A school that assigns an exam_fee with no period for the mid-term is refused 409 FEE_PERIOD_ALREADY_ASSIGNED when it tries to assign an exam_fee for the final, with a message naming 'this period' when the caller named none. The same holds for a second admission_fee or transport_fee assigned without a period. The only escapes are supplying a period_month, which no error message suggests, or waiving the first fee — status != waived is the guard's one exclusion (:273).

**Fix / open question:** Blocked on this question, which §17 does not answer: what identifies two distinct fees of the same component for the same student when neither names a period? Answering it requires the SRS to say one of three things it does not say — that period_month is mandatory on an assignment (§17 never names a period at all), that a fee with no period is exempt from the double-bill guard (which reopens the double-click double-bill the guard was written for), or that fee_structure_id distinguishes them when the period is null (which contradicts the guard's own stated reason for matching on the schema triple rather than the structure). Until that is answered, the actionable and invention-free steps are: document the null-period consequence in the module header beside the guard's existing rationale at fees.service.js:246-265, and add a suite case at verify-fees.js pinning the current behaviour so the choice is recorded rather than incidental.

**Evidence:** SRS: docs/SRS-extracted.md:906-909 the components including '- Exam Fee'; :923 'Expected Outcome: Fee Structure is available for assignment to students.'; :1665-1671 §35 'Where the source document does not specify a detail, this SRS does not decide it ... - Additional business rules'. CODE: backend/src/modules/fees/fees.service.js:266-278 alreadyAssigned() with 'period_month: periodMonth || null,' at :272 inside the where; :361-374 the call with rows[0].period_month and ApiError.conflict('Those students already carry this fee for this period', { code: 'FEE_PERIOD_ALREADY_ASSIGNED' }); :335 'period_month: dateOnly(payload.period_month) || null,'; backend/src/modules/fees/fees.validation.js:137 'period_month: fields.period_month,' (optional). Guard rationale: backend/src/modules/fees/fees.service.js:246-265. Untested null case: backend/scripts/verify-fees.js:942 and :971 both set period_month; the only period-less assignment, :871-886, is never repeated.

---

### 39 — §33 SaaS Engine SRS-extracted.md:1635 ("- Custom Pricing"); §29 SRS-extracted.md:1426 ("| subscription_overrides |"); §10.4 SRS-extracted.md:404, :452 ("Custom Price"); §35 SRS-extracted.md:1672 ("- Additional pricing rules")

The mechanism is real and I confirmed every link in the chain. A ripgrep for `SubscriptionOverride|subscription_overrides` across backend/src returns hits in models/ (definition and association), entitlementService.js, subscriptions.service.js and one stray comment in app.js:445 — and nothing at all under backend/src/modules/invoices/. entitlementService.js:501-503 skips OVERRIDE_TYPES.PRICE with the comment "it belongs to invoice generation rather than here"; invoice generation does not read it — `generateForSubscription()` builds its lines from `db.SubscriptionItem.findAll(...)` at invoices.service.js:779 and nothing else. The cycle price on the subscription row comes from `computeCycleAmount()` (subscriptions.service.js:444) reading `plan_prices`. `createOverride()` writes the row at subscriptions.service.js:2123 and validation requires `amount` when override_type is PRICE. So the write returns 201, the row is durable, it is echoed back in subscription detail (the `overrides` include at subscriptions.service.js:186), and it changes no money anywhere — not the invoice, not the renewal, not the entitlement snapshot. One correction to the finding: it says the row "is read by nothing"; it IS read back for display. Nothing acts on it, which is the substantive point and is correct.

The finding's mitigation also checks out: §10.4's Custom Price model is implemented at subscriptions.service.js:447-449 (`if (model === PRICING_MODELS.CUSTOM) return money.round(price.custom_amount);`), so §33's "Custom Pricing" is not wholly absent — it is just not delivered by the mechanism the checklist cites at :436 and :478.

Why blocked rather than fixable: the SRS says three things about this and no more. §33:1635 lists "Custom Pricing" as an MVP checklist item. §29:1426 names the `subscription_overrides` table. §10.4:404/:452 names Custom Price as one of five plan pricing models. `grep -i "override"` over the SRS returns only :1426 and §33's :1635-1637. Nothing states what a per-subscription price override does. Wiring it to invoices means choosing its semantics, and §35:1672 lists "Additional pricing rules" among what this SRS does not decide.

The documentary half is separately fixable and should be done regardless: entitlementService.js:502's forward-reference to invoice generation is false as a statement about this codebase, and checklist :436/:478 should record that a `price` override is currently accepted and inert, with §33 "Custom Pricing" delivered by §10.4's Custom Price model instead. That correction needs nothing invented. It does not close the gap of a 201-returning endpoint that changes nothing.

**Consequence:** A Super Admin can POST `{override_type:'price', target_key:'cycle_amount', amount: X}` to /subscriptions/:id/overrides, receive 201, see the override listed on the subscription detail response — and the school is still billed the plan price on the next invoice and every renewal. A negotiated price recorded through the endpoint the checklist names as the evidence for "Custom Pricing | Completed" is silently not honoured. That is a billing-correctness exposure, not a cosmetic one.

**Fix / open question:** BLOCKED on this question, which the SRS does not answer: when a `subscription_overrides` row carries `override_type='price'`, what exactly does it change — the subscription's stored per-cycle `amount`, the generated invoice's line total, or an additional invoice line — and from which cycle? How does it interact with `effective_from`/`effective_until`, with §12.3/§12.4 proration on upgrade and downgrade, with the first-cycle setup fee that `isFirstCycle` gates at invoices.service.js:776, and with add-on lines? §33:1635 names the MVP item and §29:1426 names the table, but no clause states any of these rules, and §35:1672 puts "Additional pricing rules" among the gaps this SRS declines to fill. Two candidate responses both need that decision first: wiring the override into `generateForSubscription()`, or refusing `override_type='price'` at validation the way §13.1's `issue()` was made to refuse rather than guess — the second removes a capability §33 names, so it is a decision too. What can be done now without any decision: correct entitlementService.js:502 (delete the false claim that invoice generation consumes it) and correct IMPLEMENTATION_CHECKLIST.md:436/:478 to attribute §33 "Custom Pricing" to §10.4's Custom Price model at subscriptions.service.js:447-449 and to record the override path as accepted-but-inert.

**Evidence:** SRS: docs/SRS-extracted.md:1635 "- Custom Pricing"; docs/SRS-extracted.md:1426 "| subscription_overrides |"; docs/SRS-extracted.md:404 "- Custom Price" and :452 (§10.4's five pricing models); docs/SRS-extracted.md:1672 "- Additional pricing rules". CODE (the skip and its false forward-reference): backend/src/services/entitlementService.js:501-503. CODE (invoice generation, which never reads it): backend/src/modules/invoices/invoices.service.js:779 "const items = await db.SubscriptionItem.findAll({" — grep for SubscriptionOverride under backend/src/modules/invoices/ returns nothing. CODE (the dead write): backend/src/modules/subscriptions/subscriptions.service.js:2123 "override = await db.SubscriptionOverride.create(columns, { transaction });". CODE (read back for display only): backend/src/modules/subscriptions/subscriptions.service.js:186 "model: db.SubscriptionOverride, as: 'overrides'". CODE (the path that does work): backend/src/modules/subscriptions/subscriptions.service.js:447-449. CHECKLIST: docs/IMPLEMENTATION_CHECKLIST.md:436 and :478.

---

### 47 — §5 Teacher row, SRS-extracted.md:100 vs §20.1 FR-TT-001, SRS-extracted.md:1082-1084

The SRS contradiction is real and I confirmed both halves: §5:100 says a Teacher "manages timetable-related teaching periods"; FR-TT-001, the only timetable creation requirement, names Principal / School Admin and no one else. The catalogue sided with FR-TT-001 (teacher holds `timetable.view`, not `timetable.manage`). What the SRS does not supply is which statement governs — a role-summary sentence in §5 or an FR's explicit Actor line — and the project's own constraint closes off the alternative anyway: granting `timetable.manage` to the teacher role would change the fixed 353-grant catalogue, which §35 forbids. So the deviation can only be recorded, not resolved. I found no record of it: IMPLEMENTATION_PROGRESS.md:2642 justifies the grant against FR-TT-001's actor list alone and never mentions §5's Teacher sentence, unlike the FR-ATT-003 and FR-EXAM Teacher mismatches which are explicitly "recorded rather than resolved".

**Consequence:** No runtime consequence — a teacher reads the timetable and cannot write it, which is what FR-TT-001 says. The gap is that this one §5 phrase is unsatisfied with no written trace, so a later reader can only rediscover it.

**Fix / open question:** BLOCKED — the exact question the SRS does not answer: when §5's role-responsibility prose grants a capability that the module's own FR Actor line withholds (§5:100 'manages timetable-related teaching periods' vs FR-TT-001's 'Principal / School Admin'), which governs? Until that is answered the grant cannot change (353 grants are fixed by §35). The record-only half is available without a decision: add a 'Narrower than §5's Teacher row, deliberately' note to the FR-TT-001 checklist row in the exact form FR-ATT-003 already uses at IMPLEMENTATION_CHECKLIST.md:535.

**Evidence:** SRS docs/SRS-extracted.md:100 vs :1082-1084. CODE backend/src/config/permissions.js:292-324 — the ROLES.TEACHER list contains 'timetable.view' at :309 and no 'timetable.manage'; the block's own header comment at :293-294 recites §5 including "teaching periods". RECORDED against FR-TT-001 only: IMPLEMENTATION_PROGRESS.md:2642. Contrast IMPLEMENTATION_PROGRESS.md:2602 and docs/IMPLEMENTATION_CHECKLIST.md:535.

---

### 52 — §13.3 / FR-BILL-003, SRS-extracted.md:678-687; FR-BILL-004:690

The mechanism is exactly as described: `transaction_id` is declared optional at payments.validation.js:64 and listed unqualified in the submit schema, the screenshot file is optional, and only invoice_id, amount and method are `.required()`. So a payment can land pending with nothing for the FR-BILL-004 reviewer to review. But making it required is a decision the SRS does not supply. FR-BILL-003 lists "School enters a transaction ID" and "School uploads a payment screenshot" as two steps of the same Functional Behavior; the project already read the second as optional and disclosed it, and the method allow-list is every PAYMENT_METHOD except online_gateway — which includes methods that have no transaction reference at all. Requiring the field universally would refuse a legitimate submission the SRS never says to refuse; requiring it per-method would need a method-to-requirement map the SRS does not contain.

**Consequence:** A school can submit a manual payment carrying neither a transaction ID nor a screenshot; it is recorded pending and the Super Admin's FR-BILL-004 review has no evidence to act on. Nothing breaks — approval and rejection both still work — but the review step is hollow for such a row.

**Fix / open question:** BLOCKED — the exact question: does FR-BILL-003's "School enters a transaction ID" make `transaction_id` a required field for every submittable method (cash and cheque included), or is it a step description like the screenshot upload, which this project already treats as optional? The source states no per-method rule and no requiredness. Available without a decision: extend the disclosure at IMPLEMENTATION_CHECKLIST.md:475, which currently discloses only the file's optionality, to say the transaction ID is optional too.

**Evidence:** SRS docs/SRS-extracted.md:684 "- School enters a transaction ID.", :685 "- School uploads a payment screenshot.", :690 "- Super Admin reviews the submitted transaction ID and screenshot." CODE backend/src/modules/payments/payments.validation.js:64 `transaction_id: Joi.string().trim().max(160).empty('').allow(null),`; :112-123 the submit schema — only invoice_id, amount, method carry `.required()`, `transaction_id: fields.transaction_id` is unqualified; :42-44 SUBMITTABLE_METHODS = every method except online_gateway. CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:475 discloses the file's optionality and not the transaction id's.

---

### 53 — §11.3 / FR-SUB-009 — SRS-extracted.md:494-502, :520-528

The contradiction is in two adjacent lines and I read both: :521 Description "Super Admin and/or school configure add-ons", :522 "Actor / Role: Super Admin". §11.3 itself is a bare seven-item list with no actor, so there is no third clause to break the tie. The implementation follows the Description and is internally consistent about it (requireAnyPermission with `subscriptions.self.manage`, which is seeded to principal and school_admin through SCHOOL_LEADERSHIP). That is a reading, and the SRS supplies nothing that makes it the right one. Narrowing to Super Admin alone would also strand `subscriptions.self.manage` on this route, which the router header already argues against.

**Consequence:** A principal or school_admin can purchase add-ons for their own subscription. Whether that is correct is undecidable from the source; the tenant boundary still holds either way (findById folds tenantWhere into its where).

**Fix / open question:** BLOCKED — the exact question: FR-SUB-009's Description says add-ons are configured by "Super Admin and/or school" and its Actor line, one line below, says "Super Admin". Which governs? Available without a decision: the checklist at :394 attributes the quoted phrase to "§11.3's actor", and §11.3 has no actor — re-attribute it to FR-SUB-009's Description, which is what the code comment at subscriptions.routes.js:104 already does correctly.

**Evidence:** SRS docs/SRS-extracted.md:521 vs :522; §11.3 at :494-502 assigns no actor. CODE backend/src/modules/subscriptions/subscriptions.routes.js:103-105 `const canBuyAddons = () => requireAnyPermission('subscriptions.manage', 'subscriptions.self.manage');` with the reasoning at :40-46; backend/src/config/permissions.js:236 (`subscriptions.self.manage` in SCHOOL_LEADERSHIP), applied to PRINCIPAL and SCHOOL_ADMIN at :287-290. CHECKLIST docs/IMPLEMENTATION_CHECKLIST.md:394.

---

### 60 — §33 School list SRS-extracted.md:1597-1614 vs §11.1:465-478, §20.3 FR-ASG-001:1105-1113, §21 FR-AI-001/002:1159-1173

Every fact checks out. §11.1 lists Assignments and AI among the twenty subscribable modules; FR-ASG-001's Functional Behavior is three person-at-screen steps ending "Teacher reviews the submission", and FR-AI-001 has the Teacher preview and approve while FR-AI-002's Expected Outcome is "the system displays a block/warning". §33's School MVP list is seventeen screens and contains neither, and the frontend matches §33 exactly — no assignments directory, no ai directory, and `grep -rn "ai/usage" frontend/src` returns nothing, so GET /ai/usage has no client. Which side governs is precisely what the SRS does not say: building the two screens adds surfaces §33 does not list (forbidden by §35), and leaving them out leaves FR-ASG-001's student step and FR-AI-001/002's teacher-facing steps with no place to happen. §30 Rule 3 (API-First, :1530) makes the API-only reading legitimate but does not settle it.

**Consequence:** Two FRs whose behaviour the source describes as a person acting at a screen are reachable only as API. A student cannot submit an assignment and a teacher cannot preview or approve AI questions through the product; §21's "Usage: 750 / 1000" display has an endpoint and no consumer.

**Fix / open question:** BLOCKED — the exact question: §11.1 makes Assignments and AI subscribable modules with FRs whose Functional Behavior is explicitly a person at a screen, and §33's School MVP list, introduced as "strictly per the source", contains neither. Does the MVP screen list bound the required UI surface, or do §20.3/§21's actor-facing steps require screens §33 omits? Available without a decision: state the gap in the FR-ASG-001, FR-AI-001 and FR-AI-002 rows (:569, :578, :579) rather than leaving them unqualified `Completed`.

**Evidence:** SRS docs/SRS-extracted.md:469 "- Assignments", :473 "- AI"; :1108 "- Student submits the assignment."; :1161-1162 teacher previews/approves; :1172 "the system displays a block/warning"; :1577 "strictly per the source" with the School list at :1597-1614 (Principal Dashboard → Documents, seventeen entries, neither module). CODE frontend/src/app/(school)/school/ contains attendance, classes, documents, exams, fees, finance, homework, library, parents, results, staff, students, subjects, teachers, timetable — no assignments, no ai; frontend/src/app/(student)/student/ and (teacher)/teacher/ hold only layout.tsx and page.tsx. backend/src/modules/ai/ai.routes.js:96-101 mounts GET /usage with no caller. CHECKLIST rows :569, :578, :579 all `Completed`, none records it.

---

### 63 — §23 FR-NOTIF-001 Expected Outcome, SRS-extracted.md:1224

Confirmed on every point. §23 lists nine notification types at :1207-1215 with no recipient beside any of them, and FR-NOTIF-001's Expected Outcome names five recipient classes qualified only by "as applicable to the event" — that phrase is the entire binding rule. In the engine, `grep TEACHER` over notifications.service.js returns nothing at all, so a teacher is reached by no pass; SCHOOL_BILLING_ROLES is principal + school_admin only, so the Super Admin is reached by none either; and no call site anywhere passes `schoolId: null`, so the model's own documented platform-notification convention has no writer. Which types must reach a teacher or the Super Admin is a decision the source does not make, and inventing an audience map is exactly what §35 forbids.

**Consequence:** Two of the five recipient classes FR-NOTIF-001 names receive nothing: a teacher is never notified of anything, and no platform notification is ever written for the Super Admin. Whether that is wrong is untestable, because §23 supplies no clause to test it against.

**Fix / open question:** BLOCKED — the exact question: §23 binds none of its nine notification types to any of the five recipient classes, and "as applicable to the event" is the whole of the rule. Which of the nine must reach a Teacher, and which must reach the Super Admin as a platform (null school_id) notification? Nothing in the source answers it, so every audience in the engine — including the two empty ones — is an implementation choice. Record it as such against FR-NOTIF-001 rather than as coverage; the dead convention comment at other.js:510 should say the shape is defined and unused.

**Evidence:** SRS docs/SRS-extracted.md:1224 "Relevant users (school, parent, student, teacher, or Super Admin as applicable to the event) receive the notification."; :1207-1215 the nine types with no recipients. CODE backend/src/modules/notifications/notifications.service.js:150 `const SCHOOL_BILLING_ROLES = Object.freeze([ROLES.PRINCIPAL, ROLES.SCHOOL_ADMIN]);`; :267-285 schoolAdminUserIds() resolves only those two slugs; no occurrence of ROLES.TEACHER in the file; no `schoolId: null` call site in backend/src/modules/notifications or backend/src/jobs. backend/src/models/other.js:510 "/** Null school_id = a platform notification addressed to the Super Admin. */".

---

### 64 — §11.3 Add-ons SRS-extracted.md:525; §22 SRS-extracted.md:1174-1203

Confirmed. §11.3 makes Premium Reports one of the seven add-ons and FR-SUB-009's Expected Outcome is that a subscription reflects purchased add-ons "and their effect on limits/features", but §22 defines exactly seven reports with no tier, no per-report entitlement rule and no statement of what a Premium Report is. In code the add-on resolves to a feature_unlock on key `premium_reports`, and `requireFeature` is defined and exported but mounted on no route anywhere — the only src hit outside the middleware is a prose mention in app.js. The six school reports gate on MODULES.REPORTS alone. So the add-on is purchasable and inert, and defining its effect would mean inventing the tier §22 declines to define.

**Consequence:** A school can buy the Premium Reports add-on and observe no change: the same seven reports, the same gate. Money can be charged for an entitlement that unlocks nothing.

**Fix / open question:** BLOCKED — the exact question: §22 defines seven reports and no premium tier, so what does the `premium_reports` add-on unlock — which reports, or which capability on them? The source never distinguishes a Premium Report from the seven. Until that is answered no gate can be mounted without inventing the distinction §35 forbids; record the inertness against FR-SUB-009 and §22.

**Evidence:** SRS docs/SRS-extracted.md:525 the seven add-ons including Premium Reports; :1174-1203 §22 in full, with no tiering rule. CODE backend/src/config/constants.js:286 "premium_reports: { type: 'feature_unlock', target: 'premium_reports' },"; `grep -rn requireFeature backend/src --include=*.js` → backend/src/middlewares/entitlement.js:316-326 (the definition), :474 and middlewares/index.js:62,:106 (exports), backend/src/app.js:413 (a prose comment) — no route. backend/src/modules/reports/reports.routes.js:101-110 — `requireModule(MODULES.REPORTS, report.module)` is the only entitlement gate.

---

## refused / already-fixed (6)

- **42** [already-fixed] §29 SRS-extracted.md:1518 ("No tables beyond those listed above are introduced") — None outstanding. `npm start` now refuses to bind its port if the registered models drift from §29's 64 in either direction, or if a school-scoped model is missing `school_id`, and verify-app.js asser
- **46** [refused] §2.3, SRS-extracted.md:30 vs §5:109 and §6:113 — None observable. An accurate reading of the source document with no code or checklist surface behind it.
- **48** [refused] §9 preamble SRS-extracted.md:254 vs §9.1:258, FR-SADMIN-002:302, §29:1408, §33:1 — None observable. The open question — whether a Super Admin may suspend or archive an organization, which resolveTenant then enforces platform-wide, when no FR describes an organization lifecycle — is 
- **65** [refused] §26 heading SRS-extracted.md:1322 vs body :1323-1326 and FRs :1328-1344 — None observable. The dual citation already points at the section that carries the requirement.
- **67** [refused] §25, SRS-extracted.md:1297, :1300, :1320 — None observable. No row claims coverage of either item, the four implementable §25 subjects are covered, and the source refuses the numeric target a load or timing test would require. The residue is t
- **70** [refused] §34 Requirements Traceability, SRS-extracted.md:1639-1663 — None observable. Both sections are present in the SRS and preserved; neither carries a requirement that any code or checklist row must satisfy.

## Checked only briefly (self-reported)

low-tier:58, low-tier:67
